# CHANGELOG

Политика: фиксировать только инженерно значимые изменения, влияющие на operating model, control plane, архитектуру, проверки или контракт инструментов.

## 2026-04-30

- Stage 0 для active exec-plan [computer-use-win-screenshot-first-hardening](docs/exec-plans/active/computer-use-win-screenshot-first-hardening.md) переведён в review-ready baseline: подтверждена ветка `codex/computer-use-win-screenshot-first-hardening`, выполнен harness handshake `scripts/codex/bootstrap.ps1` -> `scripts/codex/verify.ps1`, сохранён design freeze для focused `type_text` fallback, `observeAfter` successor-state и strict selector reuse без runtime/schema изменений.
- Review-gate protocol того же active exec-plan дополнительно ограничен sandbox-mode addendum для всех будущих review subagents: review prompts теперь обязаны запрещать bootstrap/verify/dotnet restore/test/build/smoke/linters и любые тяжёлые проверки без отдельного запроса, а reviewer выводы должны опираться на статический анализ diff, кода, тестов, docs и официальной документации.
- Active exec-plan [computer-use-win-screenshot-first-hardening](docs/exec-plans/active/computer-use-win-screenshot-first-hardening.md) получил stage-by-stage execution protocol по образцу completed wave plan: strict stage order, mandatory pre-commit review gate с двумя `gpt-5.5` subagents, stage report template, explicit stage mapping для `Package A-E` и отдельный full-closure stage. Дополнительно зафиксировано новое ограничение для implementer workflow: review subagents запускаются без форка истории основного implementer chat (`fork_context=false`), а весь контекст передаётся им явным prompt-ом.

## 2026-04-29

- Добавлен active exec-plan для следующего `computer-use-win` workstream: [computer-use-win-screenshot-first-hardening](docs/exec-plans/active/computer-use-win-screenshot-first-hardening.md) фиксирует current repo fit, exact source pack, official OpenAI/MCP/Microsoft constraints, file-level integration map и decision freeze для трёх связанных follow-up slices: focused poor-UIA `type_text` fallback, successor-state / action+observe и continuity UX без ослабления strict discovery proof.
- Docs/source-pack audit уточнил canonical OpenAI/MCP references и одновременно синхронизировал post-action-wave product guidance с новым real-world feedback. [stack-research](docs/generated/stack-research.md), [openai-computer-use-interop](docs/architecture/openai-computer-use-interop.md), [reference-research-policy](docs/architecture/reference-research-policy.md), [observe-capture](docs/architecture/observe-capture.md), [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [okno-roadmap](docs/product/okno-roadmap.md), [README.md](README.md), [plugins/computer-use-win/README.md](plugins/computer-use-win/README.md) и [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md) теперь явно держат canonical OpenAI source pack (`tools-computer-use`, `images-vision`, `tools-connectors-mcp`, `learn/tools`, cookbook `mcp_tool_guide`, `docs-mcp`, `codex/mcp`, `codex/app/windows`), исправляют stale `platform.openai.com` computer-use link и фиксируют четыре практических follow-up after shipped action wave: focused poor-UIA `type_text` fallback, successor-state / action+observe reduction after `verify_needed`, stronger public instance continuity UX without weakening strict discovery proof и operator screenshot preview hardening для client UIs, которые не показывают MCP image blocks inline.
- Product/docs слой дополнительно синхронизирован с shipped `computer-use-win action wave`, repo rehome в `Desktop\\Okno` и более полным набором official OpenAI sources. [README.md](README.md), [okno-roadmap](docs/product/okno-roadmap.md), [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md), [observe-capture](docs/architecture/observe-capture.md), [reference-research-policy](docs/architecture/reference-research-policy.md), [openai-computer-use-interop](docs/architecture/openai-computer-use-interop.md), [plugins/computer-use-win/README.md](plugins/computer-use-win/README.md) и [stack-research](docs/generated/stack-research.md) теперь вместе отражают три новых practical выводa: action wave уже shipped и больше не описывается как future subset; `images-vision` подтверждает `detail=\"original\"` / remap discipline для computer-use screenshots; `mcp_tool_guide`, `docs-mcp` и `codex/mcp` усиливают quiet-surface, allow-list и Docs-MCP-as-first-lookup policy без требования менять runtime архитектуру.
- Cleanup stale orchestration path после rename/rehome в Okno: backup-копия cache-installed `computer-use-win` под `.tmp/.codex/plugin-cache-backup-20260428-183853/.../.computer-use-win-repo-root.txt` больше не указывает на старый pre-Okno checkout, а резолвится в актуальный checkout `C:\Users\v.vlasov\Desktop\Okno`.

## 2026-04-28

- Product install verification для `computer-use-win` закрыл фактический `get_app_state` blocker в cache-installed plugin: [WinBridge.Runtime.Windows.UIA.Worker](src/WinBridge.Runtime.Windows.UIA.Worker/WinBridge.Runtime.Windows.UIA.Worker.csproj) теперь возвращает published sidecar artifacts абсолютными путями и явно участвует в том же `System.Text.Json` package graph, [WinBridge.Server](src/WinBridge.Server/WinBridge.Server.csproj) stage-ит self-contained UIA worker в server publish output, а [publish-computer-use-win-plugin](scripts/codex/publish-computer-use-win-plugin-core.ps1) пробрасывает self-contained worker publish property. Новый install-surface gate в [ComputerUseWinInstallSurfaceTests](tests/WinBridge.Server.IntegrationTests/ComputerUseWinInstallSurfaceTests.cs) проверяет `includedFrameworks` для worker runtimeconfig и запускает worker snapshot probe, чтобы plugin-local `WinBridge.Runtime.Windows.UIA.Worker.exe` больше не падал как framework-dependent sidecar или dependency-mismatched sidecar внутри self-contained runtime bundle.
- Product install verification для `computer-use-win` закрыл publication metadata drift: [plugin.json](plugins/computer-use-win/.codex-plugin/plugin.json) теперь описывает весь shipped public profile (`list_apps`, `get_app_state`, `click`, `press_key`, `set_value`, `type_text`, `scroll`, `perform_secondary_action`, `drag`), а [ComputerUseWinInstallSurfaceTests](tests/WinBridge.Server.IntegrationTests/ComputerUseWinInstallSurfaceTests.cs) добавил gate, который не даст plugin manifest вернуться к старому observe/click-only narrative при cache-installed publication.
- Recovery transfer hardening для нового standalone checkout зафиксировал `plugins/computer-use-win/references/AppInstructions` как коммитящийся plugin-source surface: [.gitignore](.gitignore) теперь игнорирует только root cache `/references/`, поэтому advisory playbooks, которые runtime ищет через `ComputerUseWinOptions.AppInstructionsRoot`, не теряются при clean clone / plugin publication.
- Post-closure hardening pass по external review findings устранил несколько contract/runtime/docs drifts без расширения public surface. [ComputerUseWinCoordinateSpaceContract](src/WinBridge.Server/ComputerUse/ComputerUseWinCoordinateSpaceContract.cs), [ComputerUseWinScrollContract](src/WinBridge.Server/ComputerUse/ComputerUseWinScrollContract.cs), [ComputerUseWinDragContract](src/WinBridge.Server/ComputerUse/ComputerUseWinDragContract.cs), [ComputerUseWinScrollExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinScrollExecutionCoordinator.cs), [ComputerUseWinDragExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinDragExecutionCoordinator.cs), [ComputerUseWinPerformSecondaryActionExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinPerformSecondaryActionExecutionCoordinator.cs), [ComputerUseWinSecondaryActionResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinSecondaryActionResolver.cs), [Win32InputPlatform](src/WinBridge.Runtime.Windows.Input/Win32InputPlatform.cs), [Win32InputService](src/WinBridge.Runtime.Windows.Input/Win32InputService.cs), [InputFailureStageValues](src/WinBridge.Runtime.Windows.Input/InputFailureStageValues.cs), [InputResultMaterializer](src/WinBridge.Runtime.Windows.Input/InputResultMaterializer.cs) и [plugins/computer-use-win/README.md](plugins/computer-use-win/README.md) теперь together close the confirmed follow-up gaps: whitespace-only `coordinateSpace` для `scroll`/`drag` больше не default-ится молча, trimmed `screen` path consistently интерпретируется как screen-mode end-to-end, coordinate `drag` больше не активирует окно до approval, semantic `scroll` и `perform_secondary_action` берут fresh UIA proof уже после activation boundary, confirmed risky `perform_secondary_action` сохраняет `risk_class` / `confirmation_required` markers в observability, partial `keypress` dispatch делает best-effort key-up compensation с compensated/uncompensated failure evidence, а plugin README больше не описывает shipped девяти-tool profile как future wave.
- `computer-use-win` Stage 10 shipped `drag` как полноценное public action и тем самым закрыл всю action wave. [ComputerUseWinDragContract](src/WinBridge.Server/ComputerUse/ComputerUseWinDragContract.cs), [ComputerUseWinDragTargetResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinDragTargetResolver.cs), [ComputerUseWinDragExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinDragExecutionCoordinator.cs), [ComputerUseWinDragHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinDragHandler.cs), [ComputerUseWinToolRegistration](src/WinBridge.Server/ComputerUse/ComputerUseWinToolRegistration.cs), [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs), [Program](src/WinBridge.Server/Program.cs), [InputCoordinateMapper](src/WinBridge.Runtime.Windows.Input/InputCoordinateMapper.cs), [Win32InputService](src/WinBridge.Runtime.Windows.Input/Win32InputService.cs), [Win32InputPlatform](src/WinBridge.Runtime.Windows.Input/Win32InputPlatform.cs), [InputResultMaterializer](src/WinBridge.Runtime.Windows.Input/InputResultMaterializer.cs), [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs) и [observability](docs/architecture/observability.md) теперь вместе закрывают полный promotion contour для drag: separate source/destination proof, element-to-element и point-backed endpoint modes, explicit confirmation для coordinate endpoints, factual `move -> button_down -> move/path -> button_up` dispatch, safe drag telemetry (`source_mode`, `destination_mode`, `path_point_count_bucket`, `coordinate_fallback_used`) без raw `stateToken` и raw points, deterministic helper story `get_app_state -> drag -> get_app_state` и cache-installed plugin surface с `drag` в `tools/list`. Main wave plan [completed-2026-04-28-computer-use-win-next-actions](docs/exec-plans/completed/completed-2026-04-28-computer-use-win-next-actions.md) reopened Stage 9 как pre-closure checkpoint отдельным commit, затем закрыл Stage 10 поверх prerequisite plan [completed-2026-04-28-computer-use-win-drag-prerequisites](docs/exec-plans/completed/completed-2026-04-28-computer-use-win-drag-prerequisites.md); product docs [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md), [okno-roadmap](docs/product/okno-roadmap.md) и [okno-spec](docs/product/okno-spec.md) синхронизированы: итоговый shipped public subset теперь включает `drag`, тогда как public `windows.input` по-прежнему остаётся click-first surface и не превращается в user-facing tool zoo.
- `computer-use-win` Stage 8 принял evidence-based deferral decision для `drag`. Main wave не публикует `drag` как implemented public action: [Win32InputPlatform](src/WinBridge.Runtime.Windows.Input/Win32InputPlatform.cs) всё ещё возвращает explicit unsupported drag dispatch, current `windows.input` / `computer-use-win` runtime не даёт полного source+destination proof contour, а helper/smoke harness пока не содержит deterministic public drag story. Вместо ложной promotion added bounded prerequisite ExecPlan [completed-2026-04-28-computer-use-win-drag-prerequisites](docs/exec-plans/completed/completed-2026-04-28-computer-use-win-drag-prerequisites.md), который выделил четыре узких prerequisite tracks: Win32 drag dispatch, endpoint proof, helper+smoke и только затем будущую public promotion. Main `computer-use-win` docs и roadmap синхронизированы: `drag` остаётся единственным deferred action после shipped `perform_secondary_action`.
- `computer-use-win` Stage 7 shipped `perform_secondary_action` как product-owned semantic secondary action без context-menu/right-click fallback. [ComputerUseWinPerformSecondaryActionContract](src/WinBridge.Server/ComputerUse/ComputerUseWinPerformSecondaryActionContract.cs), [ComputerUseWinSecondaryActionResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinSecondaryActionResolver.cs), [ComputerUseWinPerformSecondaryActionExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinPerformSecondaryActionExecutionCoordinator.cs), [ComputerUseWinPerformSecondaryActionHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinPerformSecondaryActionHandler.cs), [IUiAutomationSecondaryActionService](src/WinBridge.Runtime.Windows.UIA/IUiAutomationSecondaryActionService.cs), [Win32UiAutomationSecondaryActionService](src/WinBridge.Runtime.Windows.UIA/Win32UiAutomationSecondaryActionService.cs), [ComputerUseWinAffordanceResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinAffordanceResolver.cs), [ComputerUseWinActionability](src/WinBridge.Server/ComputerUse/ComputerUseWinActionability.cs), [ComputerUseWinToolRegistration](src/WinBridge.Server/ComputerUse/ComputerUseWinToolRegistration.cs), [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs), [Program](src/WinBridge.Server/Program.cs) и [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs) теперь вместе закрывают promotion contour для secondary semantic path: fresh UIA revalidation по `elementIndex`, strong affordance mapping только для `toggle`, semantic dispatch через UIA pattern state transition proof, dangerous semantic targets наследуют explicit confirmation policy, а unsupported targets больше не пробуют context-menu fallback. Helper fixture [Program](tests/WinBridge.SmokeWindowHost/Program.cs) теперь публикует checkbox state через dynamic `AccessibleName`, а live smoke story `get_app_state -> perform_secondary_action -> get_app_state` доказывает semantic toggle через public tree. Product docs [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md) и [okno-roadmap](docs/product/okno-roadmap.md) синхронизированы: shipped public subset теперь включает `perform_secondary_action`, а remaining wave сузилась до одного `drag`.
- `computer-use-win` Stage 6 shipped `scroll` как semantic-first public action поверх существующего state-backed loop. [ComputerUseWinScrollContract](src/WinBridge.Server/ComputerUse/ComputerUseWinScrollContract.cs), [ComputerUseWinScrollTargetResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinScrollTargetResolver.cs), [ComputerUseWinScrollExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinScrollExecutionCoordinator.cs), [ComputerUseWinScrollHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinScrollHandler.cs), [Win32UiAutomationScrollService](src/WinBridge.Runtime.Windows.UIA/Win32UiAutomationScrollService.cs), [Win32InputService](src/WinBridge.Runtime.Windows.Input/Win32InputService.cs), [Win32InputPlatform](src/WinBridge.Runtime.Windows.Input/Win32InputPlatform.cs), [ComputerUseWinAffordanceResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinAffordanceResolver.cs), [ComputerUseWinToolRegistration](src/WinBridge.Server/ComputerUse/ComputerUseWinToolRegistration.cs), [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs), [Program](src/WinBridge.Server/Program.cs) и [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs) теперь вместе закрывают promotion contour для semantic/container scroll: `elementIndex` path сначала требует fresh UIA revalidation и использует `ScrollPattern`, `point` path остаётся explicit coordinate fallback с обязательным confirmation gate и capture-geometry proof, `windows.input` получил factual wheel dispatch через `SendInput`, observability пишет safe markers (`scroll_direction`, `scroll_amount_bucket`, `scroll_unit`, `semantic_scroll_supported`, `fallback_used`) без raw `stateToken`, а helper fixture [Program](tests/WinBridge.SmokeWindowHost/Program.cs) публикует deterministic scroll mirror для factual smoke story `get_app_state -> scroll -> get_app_state`. Product docs [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md), [observability](docs/architecture/observability.md) и [okno-roadmap](docs/product/okno-roadmap.md) синхронизированы: shipped public subset теперь включает `scroll`, а remaining wave сузилась до `perform_secondary_action` и `drag`.
- `computer-use-win` Stage 5 shipped `type_text` как focused-writable raw input action в public profile. [ComputerUseWinTypeTextContract](src/WinBridge.Server/ComputerUse/ComputerUseWinTypeTextContract.cs), [ComputerUseWinTypeTextExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinTypeTextExecutionCoordinator.cs), [ComputerUseWinTypeTextHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinTypeTextHandler.cs), [Win32InputService](src/WinBridge.Runtime.Windows.Input/Win32InputService.cs), [Win32InputPlatform](src/WinBridge.Runtime.Windows.Input/Win32InputPlatform.cs), [ComputerUseWinToolRegistration](src/WinBridge.Server/ComputerUse/ComputerUseWinToolRegistration.cs), [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs), [ComputerUseWinActionability](src/WinBridge.Server/ComputerUse/ComputerUseWinActionability.cs), [AutomationSnapshotNode](src/WinBridge.Runtime.Windows.UIA/AutomationSnapshotNode.cs), [InputResultMaterializer](src/WinBridge.Runtime.Windows.Input/InputResultMaterializer.cs), [InputCancellationMaterializationPolicy](src/WinBridge.Runtime.Windows.Input/InputCancellationMaterializationPolicy.cs), [AuditToolContext](src/WinBridge.Runtime.Diagnostics/AuditToolContext.cs) и [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs) теперь вместе закрывают promotion contour для raw text path: optional `elementIndex`, whitespace-only payload allowed, fail-closed focused writable proof без hidden focus guessing, fresh UIA revalidation перед dispatch, writable signal через UIA read-only semantics вместо голого `ControlType`, `SendInput` unicode text path без clipboard/paste default, committed keyboard/text side-effect evidence в artifact/cancellation model, `verify_needed` как default success mode, started-audit redaction без raw `stateToken` и raw text, public `tools/list` schema и factual smoke story `get_app_state -> click -> get_app_state -> type_text -> get_app_state` на helper app. Helper fixture [Program](tests/WinBridge.SmokeWindowHost/Program.cs) дополнительно делает deterministic select-all на focus/click для стабильного type_text smoke proof, а product docs [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md) и [okno-roadmap](docs/product/okno-roadmap.md) синхронизированы: shipped public subset теперь включает `type_text`, а remaining wave сузилась до `scroll`, `perform_secondary_action`, `drag`.

## 2026-04-27

- `computer-use-win` Stage 4 shipped `set_value` как semantic-first edit action в public profile. [ComputerUseWinSetValueContract](src/WinBridge.Server/ComputerUse/ComputerUseWinSetValueContract.cs), [ComputerUseWinSetValueExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinSetValueExecutionCoordinator.cs), [ComputerUseWinSetValueHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinSetValueHandler.cs), [Win32UiAutomationSetValueService](src/WinBridge.Runtime.Windows.UIA/Win32UiAutomationSetValueService.cs), [ComputerUseWinFreshElementResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinFreshElementResolver.cs), [ComputerUseWinToolRegistration](src/WinBridge.Server/ComputerUse/ComputerUseWinToolRegistration.cs), [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs) и [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs) теперь вместе закрывают semantic set contour: discriminated `valueKind=text|number`, fresh UIA revalidation, `ValuePattern` / `RangeValuePattern` set path без blind typing fallback, started-audit redaction без raw `stateToken` и raw semantic value, public `tools/list` schema и factual smoke stories `get_app_state -> set_value(text) -> get_app_state` и `get_app_state -> set_value(number) -> get_app_state` на helper app. Product docs [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md) и [okno-roadmap](docs/product/okno-roadmap.md) синхронизированы: shipped public subset теперь включает `set_value`, а remaining wave сузилась до `type_text`, `scroll`, `perform_secondary_action`, `drag`.
- `computer-use-win` Stage 3 shipped `press_key` как первый post-click action в public profile. [ComputerUseWinPressKeyContract](src/WinBridge.Server/ComputerUse/ComputerUseWinPressKeyContract.cs), [ComputerUseWinPressKeyExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinPressKeyExecutionCoordinator.cs), [ComputerUseWinPressKeyHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinPressKeyHandler.cs), [Win32InputService](src/WinBridge.Runtime.Windows.Input/Win32InputService.cs), [Win32InputPlatform](src/WinBridge.Runtime.Windows.Input/Win32InputPlatform.cs), [ComputerUseWinToolRegistration](src/WinBridge.Server/ComputerUse/ComputerUseWinToolRegistration.cs), [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs) и [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs) теперь вместе закрывают полный promotion contour: named-key / modifier-combo contract, bounded `repeat` `1..10`, order-independent dangerous shortcut confirmation, invariant virtual-key dispatch для shortcut-баз `A-Z/0-9`, started/completed audit redaction без raw `stateToken` и raw key literal, public `tools/list` schema и factual smoke story `get_app_state -> press_key(tab) -> get_app_state` на helper app. Product docs [computer-use-win-surface](docs/architecture/computer-use-win-surface.md) и [okno-roadmap](docs/product/okno-roadmap.md) тоже синхронизированы: shipped public subset теперь включает `press_key`, а remaining wave сузилась до `set_value`, `type_text`, `scroll`, `perform_secondary_action`, `drag`.
- `computer-use-win` Stage 2 вынес общий action execution seam из click-only path и зафиксировал product-level observability owner до первой новой action promotion. [ComputerUseWinActionRequestExecutor](src/WinBridge.Server/ComputerUse/ComputerUseWinActionRequestExecutor.cs) теперь централизует stored-state resolution, outcome mapping и unexpected failure handling для `computer-use-win` actions, а [ComputerUseWinActionObservability](src/WinBridge.Server/ComputerUse/ComputerUseWinActionObservability.cs) materialize-ит best-effort `computer_use_win.action.completed` + `artifacts/diagnostics/<run_id>/computer-use-win/action-*.json` без raw `stateToken`, raw exception message и без downcast-а публичного результата при `artifact_write` / event-sink failure. [ComputerUseWinClickHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinClickHandler.cs), [ComputerUseWinActionFinalizer](src/WinBridge.Server/ComputerUse/ComputerUseWinActionFinalizer.cs), [ComputerUseWinToolResultFactory](src/WinBridge.Server/ComputerUse/ComputerUseWinToolResultFactory.cs), [AuditInvocationScope](src/WinBridge.Runtime.Diagnostics/AuditInvocationScope.cs), [AuditLog](src/WinBridge.Runtime.Diagnostics/AuditLog.cs), [ToolContractExporter](src/WinBridge.Runtime.Tooling/ToolContractExporter.cs) и [observability.md](docs/architecture/observability.md) синхронизированы в одном цикле, а existing click payload/public behavior дополнительно удержаны regression slices в `ComputerUseWinFinalizationTests`, `ComputerUseWinActionAndProjectionTests` и `ComputerUseWinArchitectureTests`.
- `computer-use-win` Stage 1 закрыл declaration gap для следующей public action wave без расширения callable surface. [ToolNames](src/WinBridge.Runtime.Tooling/ToolNames.cs), [ToolDescriptions](src/WinBridge.Runtime.Tooling/ToolDescriptions.cs), [ToolContractManifest](src/WinBridge.Runtime.Tooling/ToolContractManifest.cs), [ComputerUseWinContracts](src/WinBridge.Runtime.Contracts/ComputerUseWinContracts.cs) и [ComputerUseWinRequestContractValidator](src/WinBridge.Server/ComputerUse/ComputerUseWinRequestContractValidator.cs) теперь честно фиксируют все шесть target actions: к уже declared `type_text` / `press_key` / `scroll` / `drag` добавлены `set_value` и `perform_secondary_action`; `type_text` получил deferred `elementIndex`, а `set_value` - discriminated placeholder payload для `text|number` без ранней публикации handler/runtime path. Public implemented profile `computer-use-win` остаётся ограниченным `list_apps`, `get_app_state`, `click`, а generated contract exports синхронизируются отдельным refresh step, а не ручным патчем.
- Product/architecture docs для `computer-use-win` дополнительно синхронизированы с official OpenAI computer-use guidance и sample repos. [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md), [computer-use-win-surface](docs/architecture/computer-use-win-surface.md), [README.md](README.md), [plugins/computer-use-win/README.md](plugins/computer-use-win/README.md) и historical [openai-computer-use-interop](docs/architecture/openai-computer-use-interop.md) теперь явно фиксируют ещё несколько практических выводов: mature structured harness не нужно ломать ради built-in visual loop, `set_value` и `perform_secondary_action` являются осмысленными Windows-native semantic additions, workflow-control не должен смешиваться с public action surface, а каждая новая action promotion требует scenario-level factual proof, а не только dispatch transcript.
- ExecPlan [completed-2026-04-28-computer-use-win-next-actions](docs/exec-plans/completed/completed-2026-04-28-computer-use-win-next-actions.md) был расширен из короткого stage outline в контекстный implementation plan для полной public action wave. План теперь фиксирует current repo fit, gap в объявлении `set_value` / `perform_secondary_action`, file-level ownership, per-action semantics, official OpenAI/MCP/Microsoft constraints, reference-repo takeaways, stage delivery order, test ladder, smoke strategy и closure report format. [computer-use-win-surface](docs/architecture/computer-use-win-surface.md) тоже синхронизирован: следующая action wave везде называет все шесть target actions вместо старого four-action shorthand.
- `docs/product/okno-roadmap.md` синхронизирован с post-`windows.input` / post-`computer-use-win` repo state: срез состояния обновлён до `2026-04-27`, в базовый shipped capability set добавлен текущий public Codex-facing profile `computer-use-win`, а следующая global action wave больше не описывается старым усечённым набором `type_text/press_key/scroll/drag`. Roadmap теперь явно отражает целевой набор `press_key`, `set_value`, `type_text`, `scroll`, `perform_secondary_action`, `drag` и честно различает уже declared deferred actions от target actions, которые пока зафиксированы только в architecture + active ExecPlan.
- OpenAI computer-use interop plan архивирован как superseded: отдельный OpenAI-native adapter больше не считается активным направлением, потому что текущий Codex-facing путь уже идёт через Windows-native `computer-use-win` plugin поверх Okno engine. Архитектурный документ сохранён как historical guardrail против OpenAI-specific DTO/glue в core runtime, а дальше development trace зафиксирован в completed ExecPlan [completed-2026-04-28-computer-use-win-next-actions](docs/exec-plans/completed/completed-2026-04-28-computer-use-win-next-actions.md).
- Active ExecPlan inventory очищен от уже закрытых workstreams: bootstrap, legacy `okno` install-surface repo work, `computer-use-win` self-contained install artifact и завершённый deferred-work план перенесены в `docs/exec-plans/completed/`. После архивирования OpenAI interop как superseded в `docs/exec-plans/active/` остаётся только новая волна `computer-use-win` actions.

## 2026-04-26

- Новая planning ветка для следующей волны `computer-use-win` зафиксировала целевой public action set без немедленного расширения shipped surface. Добавлены [computer-use-win-next-actions](docs/architecture/computer-use-win-next-actions.md) и ExecPlan [completed-2026-04-28-computer-use-win-next-actions](docs/exec-plans/completed/completed-2026-04-28-computer-use-win-next-actions.md): будущий набор теперь явно включает `press_key`, `set_value`, `type_text`, `scroll`, `perform_secondary_action` и `drag`, при этом текущий public profile остаётся `list_apps` / `get_app_state` / `click` до TDD, generated docs, smoke и review proof для каждого action. Отдельный курсор агента зафиксирован как будущий локальный overlay/sidecar слой, а не как часть MCP payload или второй системный cursor.
- Post-review hardening для `computer-use-win` закрыл gap между public instance-addressable contract и фактической identity model. [ComputerUseWinIdentityModel](src/WinBridge.Server/ComputerUse/ComputerUseWinIdentityModel.cs) больше не выводит `windowId` как детерминированный хэш из live HWND/process/thread/class signals: теперь `list_apps` выдаёт runtime-owned opaque selector из discovery catalog, а [ComputerUseWinGetAppStateTargetResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinGetAppStateTargetResolver.cs) разрешает его только через сохранённый discovery snapshot и fail-close continuity proof. Соседние implicit paths тоже ужесточены: attached fallback в `get_app_state` и [ComputerUseWinStoredStateResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinStoredStateResolver.cs) больше не доверяют одной stable-identity тройке и materialize-ят `identity_proof_unavailable` / `stale_state`, если новый live window нельзя доказуемо сопоставить с исходным snapshot. Это закрывает класс silent-retarget дефектов для `windowId` и `stateToken` без расширения public surface.
- Install-surface freshness gate больше не смотрит только на `src`, plugin tree и `scripts/codex`. [ComputerUseWinInstallSurfaceTests](tests/WinBridge.Server.IntegrationTests/ComputerUseWinInstallSurfaceTests.cs) теперь включает repo-root build graph inputs (`global.json`, `Directory.Build.props`, `Directory.Packages.props`, `WinBridge.sln`, root `*.props`/`*.targets`, при наличии `NuGet.Config`) в `PublishedRuntimeBundleIsFresh`, поэтому install acceptance больше не может reuse-ить stale runtime bundle после repo-level SDK/MSBuild/package-management drift.
- Follow-up closure поверх того же review-pack развёл continuity semantics по реальным public paths. [ComputerUseWinWindowContinuityProof](src/WinBridge.Server/ComputerUse/ComputerUseWinWindowContinuityProof.cs) теперь явным образом различает strict discovery selector proof (`windowId`), attached session continuity и observed-state continuity. Это возвращает repo-local operator loop `list_apps -> get_app_state -> click -> get_app_state`: ordinary post-action title/layout drift больше не ломает attached refresh и не инвалидирует `stateToken` раньше, чем downstream click path успевает выполнить fresh UIA revalidation. Одновременно strict `windowId` semantics сохранены: snapshot drift по-прежнему fail-close-ится как discovery-scoped selector mismatch.
- Runtime-owned selector lifetime тоже усилен на уровне модели, а не параметра. [ComputerUseWinExecutionTargetCatalog](src/WinBridge.Server/ComputerUse/ComputerUseWinIdentityModel.cs) больше не коммитит `windowId` по одному окну с immediate overflow eviction; `list_apps` materialization теперь публикует batch-aware generation и защищает весь текущий batch от overflow cleanup. Это закрывает класс dead-on-arrival selectors, где response мог вернуть `windowId`, уже вытеснённый из каталога до следующего `get_app_state`.
- Freshness inventory расширен до build/analyzer config inputs, влияющих на `dotnet publish`. Помимо `global.json` и MSBuild props/targets, [ComputerUseWinInstallSurfaceTests](tests/WinBridge.Server.IntegrationTests/ComputerUseWinInstallSurfaceTests.cs) теперь учитывает `.editorconfig`, `.globalconfig`, root `*.globalconfig` и `Directory.Build.rsp`, чтобы install-surface acceptance не работал с устаревшим runtime bundle после analyzer severity / CLI build-config drift.
- Следующий follow-up в той же architectural зоне закрыл ещё три contract/lifecycle gaps. Во-первых, observed-state proof стал mode-specific: для semantic `elementIndex` path [ComputerUseWinStoredStateResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinStoredStateResolver.cs) по-прежнему допускает ordinary window drift одного instance, но coordinate click теперь проходит через более строгий capture/geometry-aware proof и не становится action-ready только по attached-like continuity. Во-вторых, selector catalog теперь защищает не только текущий generation, но и latest published discovery snapshot: follow-up issuance через explicit `hwnd` или attached fallback больше не вытесняет sibling `windowId` из последнего `list_apps` response. В-третьих, public metadata синхронизирована с реальной stateful selector model: `list_apps` больше не рекламируется как `readOnly/idempotent`, а `ToolContractManifest` переводит его из `read_only` в `session_mutation`.
- Дополнительный review-pass по той же зоне уточнил ownership между continuity proof и action prerequisites. [ComputerUseWinWindowContinuityProof](src/WinBridge.Server/ComputerUse/ComputerUseWinWindowContinuityProof.cs) больше не классифицирует отсутствующий `CaptureReference` как stale window proof: если live target всё ещё совпадает, `click(capture_pixels)` доходит до action contract и возвращает публичный `capture_reference_required` с `refreshStateRecommended=true`. Одновременно MCP annotations для stateful `list_apps` доведены до честной модели selector lifecycle: tool теперь публикуется с `destructiveHint=true`, потому что новый `list_apps` snapshot заменяет предыдущую published selector generation и может сделать старые selectors evictable.
- Selector lifecycle для `computer-use-win` теперь отделяет product-validity от bounded storage retention. [ComputerUseWinExecutionTargetCatalog](src/WinBridge.Server/ComputerUse/ComputerUseWinIdentityModel.cs) разрешает `windowId` только если entry принадлежит текущему published discovery snapshot; новый non-empty `list_apps` и empty publication сразу инвалидируют selectors предыдущего snapshot, даже если старые entries ещё физически лежат в каталоге до TTL/overflow cleanup. Follow-up issuance через explicit `hwnd` / attached fallback по-прежнему не разрушает latest published discovery batch.
- Public `session.windowId` больше не смешивает reusable discovery selector и ephemeral execution identity. `get_app_state(hwnd)` и attached fallback переиспользуют selector из current published `list_apps` snapshot при strict match; если такого selector нет, response не публикует `windowId`, а audit пишет public `window_id` только для реально опубликованного selector и отдельный internal `execution_target_id` для execution target trail.
- Public selector publication после `get_app_state` теперь phase-aware относительно activation side effects. [ComputerUseWinGetAppStateHandler](src/WinBridge.Server/ComputerUse/ComputerUseWinGetAppStateHandler.cs) больше не отдаёт pre-activation `session.windowId` вслепую: после activation result с обновлённым `WindowDescriptor` он просит [ComputerUseWinExecutionTargetCatalog](src/WinBridge.Server/ComputerUse/ComputerUseWinIdentityModel.cs) заново доказать strict current-snapshot continuity, а non-success result без descriptor fail-close-ит public selector. Если activation изменила `WindowState`, bounds/monitor metadata или другие strict discovery поля, success payload, stored state и audit не публикуют reusable `windowId`; остаётся только internal `execution_target_id`.

## 2026-04-24

- `computer-use-win` Stage 7 закрыл hardening без расширения public product surface. Добавлен compact runtime state model (`attached` / `approved` / `observed` / `stale` / `blocked`), который запрещает action-ready promotion из stale/blocked path и не позволяет трактовать approval как замену fresh observation. Одновременно `tool.invocation.completed` для `computer-use-win` переведён на safe audit payload builders: completion trail теперь сохраняет только safe fields вроде `runtime_state`, `app_id`, `window_id`, `state_token_present`, `public_reason` и artifact hints, но больше не пишет raw `stateToken` и raw low-level `reason`. По `I1` принято evidence-based решение ничего не расширять: нового подтверждённого host-risky capability boundary сверх уже существующих isolated slices сейчас нет, поэтому targeted isolation остаётся intentionally deferred.
- `computer-use-win` Stage 6 тоже закрыт decision-only: broad availability-first soft-fail policy не принимается. Текущий contract already keeps required proof truthful (`capture` и `UIA` failures остаются `observation_failed`), а узкий advisory soft-fail остаётся только для expected `ComputerUseWinInstructionUnavailableException` path, где observation result сохраняется как success + warning. Новый stage matrix и дополнительные soft-fail stages не добавлены, потому что product docs и current tests не дают strong evidence в пользу более широкого availability-first режима.
- `computer-use-win` Stage 5 закрыт decision-only: отдельный public pure observe tool пока не вводится. Текущее evidence после Stage 4 подтверждает, что `get_app_state` остаётся намеренно action-ready и side-effecting path: он по-прежнему публикуется с `readOnlyHint=false`, может мутировать approval/focus/session state и образует shipped operator loop `list_apps -> get_app_state -> click`. Отдельный `observe_app_state` не добавлен, потому что в product docs, roadmap и текущем public test surface нет явной client/model need, оправдывающей расширение product surface вторым state tool прямо сейчас.
- `computer-use-win` Stage 4 перевёл public discovery с app-representative shape на instance-addressable model. [ComputerUseWinContracts](src/WinBridge.Runtime.Contracts/ComputerUseWinContracts.cs) теперь публикует grouped `apps[]` с nested `windows[]`, где каждый visible instance получает opaque `windowId`, а `get_app_state` таргетирует конкретное окно по `windowId` или explicit `hwnd` вместо старого ambiguous `appId` selector path. Identity boundary закреплён через lightweight owner types [ComputerUseWinIdentityModel](src/WinBridge.Server/ComputerUse/ComputerUseWinIdentityModel.cs), discovery materialization в [ComputerUseWinAppDiscoveryService](src/WinBridge.Server/ComputerUse/ComputerUseWinAppDiscoveryService.cs) и target resolution в [ComputerUseWinGetAppStateTargetResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinGetAppStateTargetResolver.cs). Одновременно public tool descriptions/manifest, smoke contract assertions и installed-copy publication acceptance синхронизированы с новым `windowId` surface, а install-surface harness теперь перепубликует plugin runtime bundle, если source tree новее bundle manifest, вместо reuse stale installed copy после contract changes.
- MCP runtime/export/test baseline проекта переведён на stable spec revision `2025-11-25`. [ToolContractExporter](src/WinBridge.Runtime.Tooling/ToolContractExporter.cs) теперь публикует `protocol_version = 2025-11-25`, stdio/request literals в [McpProtocolSmokeTests](tests/WinBridge.Server.IntegrationTests/McpProtocolSmokeTests.cs), [ComputerUseWinInstallSurfaceTests](tests/WinBridge.Server.IntegrationTests/ComputerUseWinInstallSurfaceTests.cs) и [scripts/smoke.ps1](scripts/smoke.ps1) синхронизированы на тот же baseline, а generated interface exports после refresh больше не расходятся с live handshake. Одновременно protocol decisions зафиксированы тестами: `serverInfo` остаётся минимальным без `description`, manual `computer-use-win` schemas продолжают опираться на default JSON Schema `2020-12` без явного `$schema`, `icons` по-прежнему не публикуются, а `execution.taskSupport = optional` уже materialize-ится у `get_app_state` и `click`.
- `computer-use-win` Stage 2 перестал держать orchestration внутри одного transport-facing host. [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs) теперь выступает thin adapter: `list_apps`, `get_app_state` и `click` делегируются отдельным owner classes (`ComputerUseWinListAppsHandler`, `ComputerUseWinGetAppStateHandler`, `ComputerUseWinClickHandler`), discovery materialization вынесена в `ComputerUseWinAppDiscoveryService`, а shared action-state validation — в `ComputerUseWinStoredStateResolver`. Одновременно composition root в [Program.cs](src/WinBridge.Server/Program.cs) больше не собирает `computer-use-win` через per-tool `hostServices` closures: core surface публикуется через typed wrapper `ComputerUseWinRegisteredTools` с post-build bind, а integration coverage дополнена characterization test для `list_apps`, DI resolution test для новых owner classes и architectural lock на новый registration pattern.
- `computer-use-win` Stage 1 `C0/A3` freeze убрал latent public-boundary residue без расширения shipped surface: из server registration удалены top-level MCP builders/schema hooks для deferred `type_text` / `press_key` / `scroll` / `drag`, а [ComputerUseWinTools](src/WinBridge.Server/ComputerUse/ComputerUseWinTools.cs) больше не держит callable entrypoints для этой deferred action wave. Одновременно добавлены guardrail tests, которые фиксируют publication boundary как ровно `list_apps` / `get_app_state` / `click` и не позволяют latent server hooks незаметно вернуться в public `computer-use-win` profile.
- MCP-документация по репозиторию выровнена на latest official spec revision `2025-11-25`: старые ссылки на `2025-06-18` в architecture docs, generated stack research и completed exec plans заменены на актуальные official URLs. Одновременно ExecPlan [computer-use-win-deferred-work](docs/exec-plans/completed/completed-2026-04-26-computer-use-win-deferred-work.md) дополнен отдельным migration block для перехода runtime/export/tests с negotiated MCP baseline `2025-06-18` на `2025-11-25`. В план явно добавлены protocol tracks `M0-M5`, новая `Iteration 1.5`, dependency order для protocol branch и правило: latest spec links в docs не означают, что runtime уже мигрирован.
- Deferred ExecPlan [computer-use-win-deferred-work](docs/exec-plans/completed/completed-2026-04-26-computer-use-win-deferred-work.md) расширен из product-backlog файла в sequencing-aware review pack: помимо трёх already-known redesign classes документ теперь фиксирует companion hardening tracks (`C0/A1/A2/A3/CR1/S1/S2/O1/C1/I1`), dependency order, iteration sequencing и branch recommendations. Это явно поднимает ранний surface-freeze для unpublished action wave, composition-root stabilization вокруг `Program.cs` tool registration и full `publication + install surface` matrix как часть следующего `computer-use-win` цикла, а не как разрозненные follow-up заметки.
- Добавлен deferred ExecPlan [computer-use-win-deferred-work](docs/exec-plans/completed/completed-2026-04-26-computer-use-win-deferred-work.md): review findings, которые были признаны redesign / next-scope, теперь зафиксированы как отдельные будущие work items. Документ разводит instance-addressable discovery, optional pure-observe split для `get_app_state` и advisory provider failure policy redesign, чтобы следующие ветки не смешивали product/API redesign с hardening текущего shipped contract.
- Click activation failure taxonomy больше не смешивает платформенную невозможность подготовить окно с policy deny и не выводит причину failure из косвенных nullable/boolean flags. [ActivateWindowResult](src/WinBridge.Runtime.Contracts/ActivateWindowResult.cs) теперь создаётся только через typed factories: `failureKind` обязателен для `failed` / `ambiguous`, а `done` не может нести failure cause. [WindowTargetResolver](src/WinBridge.Runtime.Windows.Shell/WindowTargetResolver.cs) возвращает typed live-identity resolution, поэтому HWND reuse / stable identity drift сохраняется как `identity_changed`, а не схлопывается в `missing_target`. [ComputerUseWinClickExecutionCoordinator](src/WinBridge.Server/ComputerUse/ComputerUseWinClickExecutionCoordinator.cs) маппит source-owned cause в product semantics: missing live target остаётся `missing_target`, identity drift становится `stale_state`, свернутое окно — `target_minimized`, неподтверждённый foreground — `target_not_foreground`. Фаза сохраняется как `AfterActivationBeforeDispatch`, чтобы retry guidance и `refreshStateRecommended` оставались честными.
- Follow-up recovery hardening для `computer-use-win` закрыл remaining rollback-source ordering gap. [scripts/codex/publish-computer-use-win-plugin.ps1](scripts/codex/publish-computer-use-win-plugin.ps1) больше не перемещает `win-x64.backup-*` напрямую в canonical `runtime/win-x64` до proof: restore теперь идёт через `stage backup -> validate existing manifest -> promote validated candidate`, а original backup сохраняется до доказанного terminal state. Legacy/pre-manifest backup больше не получает manifest из неподтверждённого directory state: missing manifest fail-close-ится, чтобы partial runtime не мог стать новым source of truth. Test-only publish-source override вынесен из production entrypoint в отдельный harness wrapper [test-publish-computer-use-win-plugin.ps1](scripts/codex/test-publish-computer-use-win-plugin.ps1), поэтому обычный publish больше не зависит от ambient test env. Integration coverage расширен regression-тестами на corrupt manifest backup, pre-manifest rollback, missing managed dependency, fallback repair handoff и bounded timeout для publish PowerShell harness, чтобы зависший restore test не оставлял живые publish/test процессы.

## 2026-04-23

- Install/recovery hardening для `computer-use-win` больше не доверяет четырём sentinel-файлам как proof runnable bundle. [scripts/codex/publish-computer-use-win-plugin.ps1](scripts/codex/publish-computer-use-win-plugin.ps1) теперь формирует `okno-runtime-bundle-manifest.json` из полного published payload (`relative path + size` для каждого файла) и использует тот же manifest как source of truth для `staging`, `swap`, `repair` и canonical `runtime/win-x64`. Соседний launcher [plugins/computer-use-win/run-computer-use-win-mcp.ps1](plugins/computer-use-win/run-computer-use-win-mcp.ps1) fail-fast-ится по тому же manifest, поэтому missing dependency DLL больше не masquerade-ится под complete install. Recovery handoff из `repairRoot` также усилен: если финальный promote обратно в canonical path падает, script пытается вернуть last-known-good backup именно в `runtime/win-x64`, а не оставляет runnable bundle только во временной side directory. Одновременно public metadata `get_app_state` перестала рекламировать tool как `read_only`: registration, manifest и smoke/runtime proofs теперь честно классифицируют его как `OsSideEffect`, потому что approved/confirmed path мутирует approval/focus/session state.
- Recovery hardening для `computer-use-win` install artifact довёл canonical runtime integrity до более сильного инварианта. [scripts/codex/publish-computer-use-win-plugin.ps1](scripts/codex/publish-computer-use-win-plugin.ps1) больше не использует `runtime/win-x64` как repair scratch space: fallback restore materialize-ит backup в отдельный `win-x64.repair-*` directory, проверяет completion proof по обязательным runtime files (`Okno.Server.exe/.dll/.deps.json/.runtimeconfig.json`) и только затем handoff-ит canonical path. Synthetic failure after copying only `Okno.Server.exe` теперь доказывает, что partial tree не остаётся на canonical install path. Соседний launcher [plugins/computer-use-win/run-computer-use-win-mcp.ps1](plugins/computer-use-win/run-computer-use-win-mcp.ps1) тоже перестал доверять одному apphost и fail-fast-ится на incomplete runtime bundle.
- Follow-up hardening по следующей review-волне добил два contract seam-а без расширения surface. Во-первых, [scripts/codex/publish-computer-use-win-plugin.ps1](scripts/codex/publish-computer-use-win-plugin.ps1) теперь держит `stdout` строго payload-only: terminal JSON больше не загрязняется cleanup diagnostics, а native `dotnet publish` output не транзитит в machine-readable stream на success path; соседний install-surface harness научен читать `stdout/stderr` конкурентно, чтобы stderr diagnostics не блокировали long-running publish paths. Во-вторых, `get_app_state` target admission получил явный typed resolver [ComputerUseWinGetAppStateTargetResolver](src/WinBridge.Server/ComputerUse/ComputerUseWinGetAppStateTargetResolver.cs), который сохраняет `identity_proof_unavailable` и на attached-window fallback, а не только на explicit admission path. Это закрывает taxonomy drift между sibling selector branches без redesign `list_apps`/discovery model.
- Очередной review-hardening для `computer-use-win` довёл structured action lifecycle до более честной phase-aware модели. Для `click` появился явный owner-layer [ComputerUseWinActionLifecyclePhase](src/WinBridge.Server/ComputerUse/ComputerUseWinActionLifecyclePhase.cs): structured `failed` и `approval_required` больше не наследуют один грубый `pre-dispatch = no refresh` default, а materialize-ят `refreshStateRecommended` из сочетания lifecycle phase и public failure semantics. Это закрывает несколько drift-классов сразу: `state_required` / `stale_state` / `capture_reference_required` снова честно требуют свежий `get_app_state`, а outcomes после activation/revalidation больше не masquerade-ятся под safe no-refresh reject.
- Click boundary стал полнее как pre-activation contract owner. [ComputerUseWinClickContract](src/WinBridge.Server/ComputerUse/ComputerUseWinClickContract.cs) теперь reject-ит whitespace-only `button` / `coordinateSpace` как `invalid_request` вместо silent normalization к default left/capture path, а deterministic `capture_pixels` point вне сохранённого raster отсекается до первого `ActivateAsync()`. Одновременно click resolution path перестал трактовать blank enum-like fields как omission и продолжает fail-close policy для semantic target admissibility.
- `get_app_state` больше не маскирует transient identity-proof failure под policy block. В public contract добавлен `identity_proof_unavailable`, а [ComputerUseWinGetAppStateFinalizer](src/WinBridge.Server/ComputerUse/ComputerUseWinGetAppStateFinalizer.cs) теперь materialize-ит этот outcome отдельно от `blocked_target`, чтобы approval/block taxonomy не путала retriable отсутствие stable process identity с намеренным запретом automation.
- Publish/install recovery path доведён до recovery-first semantics без ложных cleanup failures. [publish-computer-use-win-plugin.ps1](scripts/codex/publish-computer-use-win-plugin.ps1) теперь отделяет correctness-critical promote/restore от maintenance-only cleanup: `staging/swap/backup` cleanup стал best-effort, не переводит уже successful promote в non-zero failure и при restore-move failure делает copy-based repair canonical `runtime/win-x64` перед rethrow исходной ошибки publish. Integration coverage расширен новыми proof-ами на cleanup-failure-after-promote и restore-failure-with-canonical-runtime-repair.

## 2026-04-21

- Дополнительный review-hardening довёл `get_app_state` до явного commit-on-success pipeline. `ComputerUseWinAppStateObserver` больше не пишет `stateToken` в store самостоятельно: он готовит critical observation result (`capture + UIA + stored state + observation envelope`), а `ComputerUseWinTools` materialize-ит payload, создаёт `CallToolResult` и только после этого commit-ит token/store entry и attached session. Это устраняет ghost tokens и eviction valid tokens на поздних failure-path после otherwise-successful observation.
- Advisory enrichment отделён от critical observation semantics. `ComputerUseWinPlaybookProvider` теперь живёт за small instruction-provider seam, а observer загружает instructions как fail-soft advisory stage: проблемы чтения plugin-local playbook hints больше не downcast-ят `get_app_state` в `failed + observation_failed`, а дают пустой `instructions` плюс warning при сохранённом successful observation result.
- Ещё один review-hardening проход закрыл остаточные boundary gaps в `computer-use-win`. `stateToken` теперь хранит explicit observation envelope (`depth/maxNodes`), а `click(elementIndex)` revalidation использует тот же UIA node budget, на котором элемент был изначально опубликован, вместо silent fallback на defaults. Это убирает ложный `stale_state` для deep-tree elements и фиксирует инвариант “revalidation must not be weaker than original observation”.
- Public request boundary для `computer-use-win` стал contract-aware не только на top-level binding, но и на nested/runtime-relevant schema semantics. `ToolRequestBinder` теперь поддерживает post-bind contract validation, `ComputerUseWinRequestContractValidator` enforce-ит schema-invalid `maxNodes`, nested extra fields в `point/fromPoint/toPoint` и scalar minima, а explicit blank `--tool-surface-profile` больше не эквивалентен отсутствующему profile selector и fail-close-ится на старте. Соседний `WindowTools` path с `JsonExtensionData` сохранён и отдельно перепроверен regression tests.
- Observation-stage failures для public `computer-use-win` paths доведены до единой structured materialization policy. `ComputerUseWinAppStateObserver` и `ComputerUseWinClickTargetResolver` теперь оба переводят thrown observation exceptions в typed `failed + observation_failed` вместо transport leakage, при этом genuine semantic mismatch после успешного fresh snapshot по-прежнему остаётся `stale_state`. Это выравнивает `get_app_state` и click revalidation с уже существующим sibling pattern `windows.uia_snapshot`.
- Review hardening для `computer-use-win` закрыл несколько реальных drift-классов без возвращения к compatibility слоям. Public profile selection теперь fail-close-ится через shared resolver вместо silent widening в `windows-engine`; canonical app/process identity сведена к одному bare process name без `.exe`, поэтому block policy, playbooks, `appId` и approval store больше не сравнивают несовместимые string forms. Одновременно request binding вынесен в shared strict helper: omitted MCP `arguments` теперь честно нормализуются в пустой request object, а `computer-use-win` request DTOs начинают reject-ить unknown JSON fields в соответствии с published `additionalProperties: false`, не ломая соседний `WindowTools` pattern с `JsonExtensionData`.
- Public `computer-use-win` state/action loop стал честнее на boundary необратимого действия. `get_app_state` теперь возвращает action-ready `ok` только после успешного capture + UIA observation, сохраняет `HasKeyboardFocus`, не выдаёт `stateToken` и не commit-ит attached session на failed observation path, а capture/UIA failures materialize-ятся как structured `failed + observation_failed` вместо quiet `ok` с warnings или transport exception. Для `click` введён отдельный pre-dispatch target resolver: `elementIndex` пере-подтверждается по свежему UIA snapshot, low-confidence coordinate click требует explicit confirm, а stale/mismatched semantic target fail-close-ится как `stale_state`.
- Local product state стал bounded и corruption-tolerant. `ComputerUseWinStateStore` получил TTL + max-entry eviction вместо неограниченного накопления state tokens, а `ComputerUseWinApprovalStore` теперь переживает битый JSON как empty approval set, пишет через temp-file + atomic replace и не роняет public tools при write-path failures, оставляя approval хотя бы в памяти текущего процесса.
- `computer-use-win` install architecture переведена с checkout-dependent dev trampoline на self-contained plugin-local runtime artifact. Новый script [scripts/codex/publish-computer-use-win-plugin.ps1](scripts/codex/publish-computer-use-win-plugin.ps1) публикует `Okno.Server` как `win-x64` self-contained apphost в [plugins/computer-use-win/runtime/win-x64](plugins/computer-use-win/runtime/win-x64), а public launcher [plugins/computer-use-win/run-computer-use-win-mcp.ps1](plugins/computer-use-win/run-computer-use-win-mcp.ps1) теперь стартует только этот plugin-local `Okno.Server.exe --tool-surface-profile computer-use-win` и fail-fast-ит, если runtime bundle не подготовлен.
- Из public install path удалены repo-root hint и checkout trampoline для `computer-use-win`: больше нет зависимости на `resolve-computer-use-win-repo-root.ps1`, `write-computer-use-win-plugin-repo-root-hint.ps1`, `.computer-use-win-repo-root.txt` и `.tmp/.codex/artifacts/local` как product runtime source. Internal/dev-only `okno` install path пока сохраняет свой отдельный hint-based narrative.
- Новый integration coverage доказывает install contract именно на plugin boundary: publish script materialize-ит plugin-local runtime bundle, launcher fail-fast-ит без `runtime/win-x64/Okno.Server.exe`, а temp plugin copy вне repo tree и без env/hint dependency поднимает ровно public `list_apps/get_app_state/click` surface. Docs и generated command inventory синхронизированы с новым publish/install flow.
- Исправлен plugin launch-path drift для `computer-use-win` и internal `okno`: корневая причина была не в самом `computer-use-win` profile, а в том, что plugin launcher сначала молча резолвил runtime из fallback build cache (`src/**/bin`), а затем мог reuse-ить stale staged manifest, тогда как integration proofs уже работали от fresh staged `.tmp/.codex/artifacts/local`. Из-за этого installable plugin поднимал stale internal `windows.*` surface вместо public `list_apps/get_app_state/click`.
- Добавлен [scripts/codex/resolve-okno-server-launch-target.ps1](scripts/codex/resolve-okno-server-launch-target.ps1), а launcher-ы [plugins/computer-use-win/run-computer-use-win-mcp.ps1](plugins/computer-use-win/run-computer-use-win-mcp.ps1) и [plugins/okno/run-okno-mcp.ps1](plugins/okno/run-okno-mcp.ps1) теперь pin-ят explicit `artifacts_root`, materialize-ят fresh bundle и предпочитают staged apphost `Okno.Server.exe`; `dotnet + .dll` остаётся только fallback без apphost.
- Добавлен regression proof в [tests/WinBridge.Server.IntegrationTests/RuntimeBundleResolverTests.cs](tests/WinBridge.Server.IntegrationTests/RuntimeBundleResolverTests.cs), а docs/commands surface синхронизирован с новым Windows-native launch target resolver.

## 2026-04-20

- Репозиторий начал product realignment к `computer-use-win` как главному публичному Codex surface без второго runtime и без отдельного репозитория. Добавлен profile-aware server start: `Okno.Server.dll` теперь понимает `--tool-surface-profile computer-use-win`, `ToolContractManifest` и exporter поддерживают отдельный public profile, а public operator subset публикуется как `list_apps`, `get_app_state`, `click`. При этом `type_text`, `press_key`, `scroll` и `drag` сознательно не объявлены shipped: они закреплены как следующий глобальный action wave, а в коде пока оставлены только touchpoints/stubs без готового runtime path.
- Добавлен новый public plugin root [plugins/computer-use-win](plugins/computer-use-win): marketplace переведён на `computer-use-win-local`, plugin manifest и launcher используют product identity `Computer Use for Windows`, developer metadata выровнена на `Vlasov Vitaly`, а install surface получил отдельные repo-root hint artifacts (`resolve-computer-use-win-repo-root.ps1`, `write-computer-use-win-plugin-repo-root-hint.ps1`, `.computer-use-win-repo-root.txt`). Старый `plugins/okno` переведён в internal/dev-only narrative и больше не описывается как главный product-facing front door.
- Public profile получил первые реальные integration proofs в [McpProtocolSmokeTests.cs](tests/WinBridge.Server.IntegrationTests/McpProtocolSmokeTests.cs): `tools/list` публикует только curated operator tools, `get_app_state` честно требует app approval до первого state response и после подтверждения возвращает `stateToken + image/png + compact accessibility tree`, а `click` работает через `stateToken + elementIndex` и использует public activation-preflight вместо перекладывания low-level `activate_window` шага на агента. Дополнительно harness stabilisation убрала лишний helper-process leakage и добавила short settle-delay после materialization helper window.
- Product/docs слой выровнен под новую границу: [README.md](README.md), [docs/product/index.md](docs/product/index.md), [docs/product/okno-roadmap.md](docs/product/okno-roadmap.md), [docs/architecture/openai-computer-use-interop.md](docs/architecture/openai-computer-use-interop.md), [docs/architecture/computer-use-win-surface.md](docs/architecture/computer-use-win-surface.md), plugin READMEs и generated docs теперь разделяют `computer-use-win` как public plugin surface и `Okno` как внутренний Windows-native engine. `refresh-generated-docs.ps1` дополнительно экспортирует отдельные generated contract snapshots для public profile в `docs/generated/computer-use-win-interfaces.*`.
- Generated research snapshot синхронизирован после shipped `windows.input`: [docs/generated/stack-research.md](docs/generated/stack-research.md) больше не описывает input как целиком deferred slice, а отделяет implemented click-first `windows.input` от deferred clipboard, semantic actions и broad input actions beyond click-first.
- После shipped `windows.input` exec-plan архивирован из `docs/exec-plans/active` в [docs/exec-plans/completed/completed-2026-04-20-windows-input.md](docs/exec-plans/completed/completed-2026-04-20-windows-input.md). Source-of-truth regression test [WindowsInputPackageDSourceOfTruthTests.cs](tests/WinBridge.Runtime.Tests/WindowsInputPackageDSourceOfTruthTests.cs) и changelog ссылки обновлены на completed-path, чтобы `active` снова отражал только живые workstream-ы.

## 2026-04-19

- Review follow-up закрыл два реальных provenance-класса без возврата к broad refactor ради старого surface. Во-первых, `windows.input` больше не принимает observed provenance как чистую геометрию: explicit target admission теперь использует attached stable identity как дополнительный anchor, если request повторно заходит в тот же `hwnd`, а `capture_pixels` copy-through bridge несёт `captureReference.targetIdentity` (`hwnd/processId/threadId/className`) и fail-close-ит не только frame/raster/DPI drift, но и same-geometry wrong-target reuse. Capture-side post-WGC refresh теперь строит refreshed target из live window snapshot, проверяет stable identity вместе с frame/raster/DPI, когда identity подтверждаема, и держит отдельную eligibility-модель для input-compatible bridge: отсутствие stable identity больше не ломает observe capture, но runtime не публикует `captureReference`, а `windows.input(capture_pixels)` fail-close-ит позже. Это закрывает mis-targeting подкласс для canonical `attach/capture -> input` loop без изменения shipped click-first action subset и без hidden focus/capture fallback. Во-вторых, staged bundle resolver больше не собирает server/helper как два независимых latest-файла: source context теперь выбирается как один coherent relative output subtree, manifest хранит `sourceContextRelativePath`, а verification path при вызове от integration test assembly pin-ит bundle к тому же relative build context вместо абстрактного `artifacts_root`. `scripts/test.ps1` дополнительно перестал выводить bundle provenance из newest test DLL: verification context теперь pin-ит bundle и `dotnet test` к одному `Configuration=Debug`, поэтому stale Release output не может перетянуть staged bundle при Debug test run. Тем самым закрыт mixed-provenance класс для smoke/fresh-host proof, где раньше можно было получить bundle из разных output-папок.
- Follow-up hardening сохранил единый post-WGC acquisition invariant и truthful verification boundary. `windows.wait(condition=visual_changed)` больше не ослабляет geometry gate относительно `windows.capture`: visual probe снова fail-close-ит frame/raster/DPI drift по тому же acquisition-basis policy, при этом `WindowDescriptor.Bounds` остаётся frame-only DTO surface для wait/runtime consumers. Одновременно `scripts/codex/verify.ps1` снова является честным каноническим proof entrypoint: `scripts/ci.ps1` больше не читает ambient `WINBRIDGE_CI_SKIP_*`, поэтому plain verify не может стать false-green из-за наследованных env flags и всегда выполняет advertised bootstrap/build/test/smoke/generated-docs contour.
- Review follow-up для Package E закрыл post-WGC acquisition boundary: `windows.capture(scope=window)` больше не строит `captureReference` geometry из live `TryResolveTarget` после получения pixels. Window WGC payload теперь сохраняет acquisition-time raster/frame/DPI basis, post-capture refresh только валидирует отсутствие frame/raster/DPI drift и fail-closes при расхождении. `WindowDescriptor.Bounds` возвращён к frame-only semantics для wait/runtime consumers, а producer и consumer теперь делят один edge-based drift predicate вместо разных origin/extent vs edge checks. `WgcCaptureOutcome` стал disposable bitmap lease, поэтому geometry validation, PNG encoding и visual wait sample materialization выполняются внутри одного ownership scope, а bitmap освобождается и при geometry fail-close. Добавлены regression tests на acquisition-basis selection, frame/raster/DPI drift rejection и bitmap detach/disposal ownership.
- Review follow-up для Package E закрыл observe-to-action geometry gap у `windows.input` без регрессии общего `windows.capture` observe surface. `windows.capture(scope=window)` теперь публикует dedicated `captureReference`, который reuse-ит exact `InputCaptureReference` shape для прямого copy-through в `capture_pixels`, а public observe metadata `bounds` / `frameBounds` сохраняют исторический `Bounds` wire shape с derived `width` / `height`. Window WGC path больше не якорит raster/content bounds к Win32 frame origin: capture layer использует доказанный visible-frame origin внутри `frameBounds` и fail-closes, если content size меньше frame, но origin не доказан. Capture-side serialization и input-side validation теперь делят один `CaptureReferenceGeometryPolicy`, поэтому canonical payload bridge и consumer stale-check используют один geometry invariant. Mapper больше не использует глобальный `32px` лимит frame/content delta; smoke берёт `captureReference` прямо из capture payload без ручной нормализации. Изменение не расширяет action surface и не добавляет hidden capture/focus в `windows.input`.

## 2026-04-17

- Package E для `windows.input` закрыт в click-first объёме без расширения action surface. Existing `scripts/smoke.ps1` теперь доказывает real helper flow через staged bundle: explicit attach/activate, fresh window capture, UIA planning по `Smoke query input`, `windows.input` click в `capture_pixels`, `verify_needed` payload + `input/input-*.json` + `input.runtime.completed`, затем `windows.wait(focus_is)` подтверждает фокус на textbox. Smoke также стартует fresh staged MCP host после recycle, проверяет `windows.input` в `tools/list`, implemented input policy в `okno.contract` и fresh missing-target input result без warm in-process state. Smoke обнаружил и закрыл coordinate-mapper blocker между WGC window capture content bounds и Win32 live frame bounds: `captureReference.frameBounds` теперь фиксирует capture-time frame basis, `capture_pixels` маппится через explicit content/frame model и продолжает fail-closed при origin drift, small/large resize, stale DPI, missing frame basis для WGC delta или out-of-bounds point. После этого `windows.input` поднят в `SmokeRequired=true`; product/architecture/generated docs синхронизированы только для shipped subset `move`, `click`, `double_click`, `click(button=right)`, а `drag`, `scroll`, `type`, `keypress`, `hotkey` и `paste` остаются deferred.
- Package D для `windows.input` добавил observability поверх уже готовых Package B/C semantics без изменения public schema, shipped subset, gate behavior или `SmokeRequired`. `Win32InputService` теперь materialize-ит factual runtime results через runtime-owned `InputResultMaterializer`: `structuredContent.artifactPath` указывает на `artifacts/diagnostics/<run_id>/input/input-*.json`, а `events.jsonl` получает best-effort `input.runtime.completed` с safe scalar metadata, `failure_stage`, `exception_type` и `committed_side_effect_evidence`. Artifact содержит sanitized request summary, target summary, factual top-level result и per-action factual results без raw exception message, raw typed text, `key`/`keys` или future keyboard payloads. Partial dispatch taxonomy Package B сохранена отдельными stages `click_dispatch_clean_failure`, `click_dispatch_partial_compensated`, `click_dispatch_partial_uncompensated`; artifact/event write failures не downcast-ят factual input result, а server boundary остаётся passthrough plus `tool.invocation.completed`.
- Review follow-up для Package D закрыл два drift-класса без изменения public `windows.input` semantics. Runtime helper boundary теперь переносит semantic `failure_stage` вместе с factual failure через dispatch-plan refresh helpers, поэтому cancellation после refreshed cursor move materialize-ится как `cancellation_after_committed_side_effect`, а не generic `input_dispatch`; соседние refreshed cursor move failures и stable-point mismatch paths сохраняют `cursor_move` / coordinate mapping. Одновременно `ToolContractManifest.ContractNotes` синхронизирован с Package D: public `okno.contract` больше не говорит, что input artifacts/events/materializer остаются follow-up, и явно оставляет только smoke/fresh-host acceptance на Package E.
- Interop docs follow-up закрыл оставшийся source-of-truth drift для OpenAI compatibility track: active OpenAI computer-use interop plan и architecture doc теперь говорят, что Package D observability для `windows.input` already landed через `input.runtime.completed` и `input/input-*.json`, а pending остаётся только Package E smoke/fresh-host acceptance. Добавлен regression guard, который связывает active `windows-input` status, interop docs, manifest notes и generated input artifact inventory, чтобы следующий lifecycle promotion не обновлял только часть declared source-of-truth слоя.
- Package C `windows.input` дополнительно зафиксирован как строгая destructive-tool materialization boundary. Deterministic request/subset/target failures теперь materialize-ятся до shared gate, поэтому `invalid_request`, `missing_target`, `stale_explicit_target` и `stale_attached_target` больше не могут быть замаскированы `needs_confirmation` / readiness path; rejected gate payload сохраняет effective target snapshot, а exception в pre-gate target resolver возвращает structured `failed + target_preflight_failed`, не раскрывая internal exception. Audit request summary ограничивает action projection до public batch лимита и публикует `actionCount/truncated`, а factual input failures используют best-effort sanitized completion, чтобы сбой audit append не мог отнять уже сформированный `InputResult`. Raw `IInputService` снова internal и доступен server/runtime friends только как DI seam, а source-of-truth docs синхронизированы с фактом implemented click-first boundary при deferred Package D/E.
- Contract propagation для нового `windows.input` failure literal доведён до source-of-truth: `target_preflight_failed` добавлен в active ExecPlan failure-code set, а `InputContractAndPolicyTests` теперь фиксирует полный набор public `InputFailureCodeValues` через reflection, чтобы следующий observable failure literal требовал осознанного обновления contract docs/tests.
- Review hardening для Package C `windows.input` закрыл ещё два boundary-класса без захода в Package D/E. Contract-owned audit summary теперь является malformed-safe pre-validation projector: `null` action elements, rejected `type` / `coordinateSpace` / `button` literals и nested DTO `JsonExtensionData` больше не превращаются ни в pre-gate crash, ни в leakage внутри `tool.invocation.started`. Runtime failure taxonomy также разведена точнее: `InputExecutionFailureException` используется для factual payload после committed side effect текущего action, setup failure после already completed previous actions materialize-ит batch-level partial result без `FailedActionIndex`, а clean first-action setup fault остаётся generic exception path и не overstating-ит partial side effect.

## 2026-04-16

- Package C для `windows.input` перевёл tool из deferred public descriptor в implemented MCP boundary без захода в Package D/E. Public registration теперь публикует только честный click-first schema subset (`move`, `click`, `double_click`, `click(button=right)`), `WindowTools` гонит все вызовы только через shared `RunGatedAsync(...)` и существующий `IInputService`, фиксирует attached target на request snapshot для implicit input path и передаёт в audit только contract-owned subset-safe request summary, чтобы rejected keyboard-like payloads и nested `JsonExtensionData` из `point` / `captureReference` / `bounds` не утекали в `tool.invocation.started`. Malformed nested `actions[]` payloads materialize-ятся как tool-level `failed + invalid_request` вместо transport/binder error, а unexpected current-action runtime faults теперь идут через typed factual failure carrier: public payload сохраняет `CompletedActionCount` / `FailedActionIndex` / per-action details без ложного reuse предыдущего committed action, при этом `tool.invocation.completed` по-прежнему сохраняет `exception_type` для расследования до Package D. В том же цикле `ToolContractManifest`, `okno.contract`, `okno.health`, exporter/generated interfaces и server/runtime boundary tests синхронизированы с новым implemented lifecycle, при этом `SmokeRequired` для `windows.input`, runtime artifacts/events и fresh-host acceptance сознательно оставлены на Package E и Package D соответственно.
- Stabilization S0 вернул честный shared MCP boundary перед дальнейшим rollout `windows.input`. Корневой defect находился не в `okno.health`, `windows.launch_process` или `windows.open_target` как таковых, а в общем readiness probe для async-state baseline: `InputAsyncStateReadabilityProbe` объявлял Win32 P/Invoke helper methods с локальными `*Native` именами без `EntryPoint`, поэтому live runtime падал `EntryPointNotFoundException` ещё до materialization readiness/gate/result envelope. После явной привязки `GetCurrentThreadId`, `GetThreadDesktop`, `OpenInputDesktop` и `CloseDesktop` shared guard snapshot снова materialize-ится честно, `okno.health` перестал уходить в top-level error-path, а malformed `launch/open` sibling cases снова доходят до уже существующего tool-level `failed + structuredContent` boundary без отдельного pre-gate hardening. В том же цикле добавлен узкий runtime test на real Win32 probe no-throw semantics, а канонический контур подтверждён зелёными `scripts/test.ps1`, `scripts/smoke.ps1` и `scripts/codex/verify.ps1`.
- Verification control plane больше не смешивает mutable build cache с runtime launch surface. Official scripts теперь используют `.NET artifacts output` root под `.tmp/.codex/artifacts/<run-id>` и отдельный staged run bundle под `.tmp/.codex/runs/<run-id>/test-bundle`, из которого integration tests, smoke и generated-docs export поднимают `Okno.Server` и `WinBridge.SmokeWindowHost`. Из-за этого `src/**/bin`, `tests/**/bin` и `obj` перестают быть execution surface для долгоживущих repo-local процессов, а file locks на `WinBridge.Server/bin` больше не являются архитектурной нормой verification loop.
- Для этого добавлен repo-level helper [scripts/codex/prepare-okno-test-bundle.ps1](scripts/codex/prepare-okno-test-bundle.ps1), `scripts/common.ps1` получил единый execution context и env wiring (`WINBRIDGE_REPO_ROOT`, `WINBRIDGE_ARTIFACTS_ROOT`, `WINBRIDGE_RUN_ROOT`, bundle manifest/env paths), `scripts/build.ps1`, `scripts/test.ps1`, `scripts/smoke.ps1`, `scripts/refresh-generated-docs.ps1` и `scripts/ci.ps1` переведены на новый контур, а старый resolver [scripts/codex/resolve-okno-server-dll.ps1](scripts/codex/resolve-okno-server-dll.ps1) больше не сканирует `src/WinBridge.Server/bin` и читает только staged bundle manifest.
- MCP integration smoke синхронизирован с новым launch surface: [McpProtocolSmokeTests.cs](tests/WinBridge.Server.IntegrationTests/McpProtocolSmokeTests.cs) теперь резолвит server/helper через env или staged bundle manifest и перестал предполагать старый `bin/Debug` layout, а repo root resolution внутри tests больше не завязано на фиксированное число parent directories. В том же цикле [docs/exec-plans/completed/completed-2026-04-20-windows-input.md](docs/exec-plans/completed/completed-2026-04-20-windows-input.md), [AGENTS.md](AGENTS.md), generated commands и test matrix выровнены под правило “build cache != runtime surface”.
- Control plane staging дополирован до общего artifact-resolution инварианта. `scripts/common.ps1` теперь выбирает для bundle не первый попавшийся root, а согласованный `server + helper` source context по freshness с deterministic tie-breaker, поэтому старый `.tmp/.codex/artifacts/...` больше не маскирует более свежий fallback build и bundle не смешивает разные provenance contexts. Одновременно integration harness и [scripts/codex/resolve-okno-server-dll.ps1](scripts/codex/resolve-okno-server-dll.ps1) больше не имеют manifest-only dead-end: при отсутствии default staged manifest они materialize-ят тот же канонический bundle через `prepare-okno-test-bundle.ps1`, а не откатываются к запуску из mutable `bin`.
- Phase boundary `restore -> build` снова зафиксирована явно: `scripts/common.ps1` теперь держит парный restore/build helper с одинаковым `--artifacts-path`, `scripts/bootstrap.ps1` пишет assets в тот же run-scoped artifacts root, а `scripts/build.ps1` вместе с [scripts/refresh-generated-docs.ps1](scripts/refresh-generated-docs.ps1) остаются на `dotnet build WinBridge.sln --no-restore` без возврата implicit restore/network dependency в verification loop.
- Bundle resolution semantics сведена к одному execution-context resolver. Новый helper [scripts/codex/resolve-okno-test-bundle.ps1](scripts/codex/resolve-okno-test-bundle.ps1) и shared policy в [scripts/common.ps1](scripts/common.ps1) теперь одинаково трактуют `run id`, `run root`, `artifacts root`, assembly-derived provenance, explicit env overrides и default auto-materialization. За счёт этого direct `dotnet test --artifacts-path .tmp/.codex/artifacts/<run-id> --no-build` больше не уезжает в `local`, [scripts/test.ps1](scripts/test.ps1) сначала определяет effective verification context и только потом stage-ит bundle, а explicit `WINBRIDGE_TEST_BUNDLE_MANIFEST` / `WINBRIDGE_SERVER_DLL` / `WINBRIDGE_SMOKE_HELPER_EXE` в resolver paths теперь fail-fast и не autorecover-ятся молча.
- Execution-context resolver дополирован ещё на двух ветках: shared derivation теперь умеет выводить `runId` и `runRoot` прямо из explicit `-ArtifactsRoot`, а reuse existing manifest больше не считается валидным только по `RunId`/`ArtifactsRoot`/`SourceContextName`. Resolver пересчитывает current source context и сравнивает source directories вместе с freshness markers (`sourceOldestWriteUtc` / `sourceNewestWriteUtc`), поэтому stale staged copy после нового build в том же run больше не reuse-ится скрыто и direct PowerShell helper path через `-ArtifactsRoot` materialize-ит run-scoped bundle уже под правильным `<run-id>`, а не под `local`.
- Bundle entry points и precedence contract доведены до полного shared behavior. Прямой [scripts/codex/prepare-okno-test-bundle.ps1](scripts/codex/prepare-okno-test-bundle.ps1) больше не инициализирует `runId`/`runRoot` локально и теперь берёт их из того же `Resolve-WinBridgeEffectiveExecutionContext(...)`, что и остальные resolver paths, поэтому `-ArtifactsRoot .tmp/.codex/artifacts/<run-id>` materialize-ит bundle под matching `runs/<run-id>/...`. Одновременно `Resolve-WinBridgeBundleResolution(...)` получил явный precedence contract: explicit parameters (`-ManifestPath`, context args) сильнее ambient session env, explicit manifest source сильнее ambient DLL pair, а ambient `WINBRIDGE_SERVER_DLL` / `WINBRIDGE_SMOKE_HELPER_EXE` используются только когда caller не задал более сильный explicit resolution request. Из-за этого `-ManifestPath` больше не затеняется остаточными env vars после `Use-OknoTestBundle`, и соседние direct context paths (`-ArtifactsRoot`, `-RunId`, `-RunRoot`) тоже не short-circuit-ятся stale ambient DLL overrides.
- Последняя итерация control-plane fixes закрыла request-level propagation drift в shared resolver. Корневая проблема оказалась не только в precedence-ветках, а в том, что wrapper/entry point код передавал optional PowerShell параметры как `-Param $null`, и binder интерпретировал это как explicit empty override. Из-за этого assembly-derived provenance path терял `artifacts_root`, а partial explicit requests могли собирать context из несовместимых слоёв. Теперь `scripts/common.ps1` строит `Resolve-WinBridgeEffectiveExecutionContext(...)` только из реально переданных request fields, `AssemblyBaseDirectory` трактуется как explicit provenance input сильнее ambient DLL env, а direct integration harness перешёл на `ProcessStartInfo.ArgumentList`, чтобы `AppContext.BaseDirectory` не деградировал в quoting-sensitive raw command line. Контракт зафиксирован новыми integration tests [RuntimeBundleResolverTests.cs](tests/WinBridge.Server.IntegrationTests/RuntimeBundleResolverTests.cs), которые доказывают и explicit `RunId` isolation от ambient artifacts state, и assembly-derived resolution поверх ambient DLL overrides.
- Следующая итерация bundle control plane довела request semantics до типизированной модели вместо одного boolean-флага. `scripts/common.ps1` теперь явно различает identity/provenance inputs (`RunId`, `RunRoot`, `ArtifactsRoot`, `AssemblyBaseDirectory`) и context-local selection input (`PreferredSourceContextName`). Из этого следуют два новых инварианта. Во-первых, противоречивые explicit context combinations больше не merge-ятся молча: shared validation layer fail-fast останавливает `RunId`/`RunRoot`/`ArtifactsRoot`/`AssemblyBaseDirectory`, если они выводят разные `runId`. Во-вторых, `PreferredSourceContextName` больше не сбрасывает active run context до `local`: он влияет только на выбор source context внутри уже выбранного run context, но всё ещё остаётся explicit bundle-resolution request и потому не проигрывает ambient manifest/DLL state. Regression contour расширен новыми integration proofs для conflict rejection и prepare-helper path с preferred source selection.
- Typed request semantics дополирована ещё на двух границах. Первая: `RunRoot` больше не считается identity-bearing path по одному только leaf имени. Shared resolver выводит `runId` из `RunRoot` только для canonical `.tmp/.codex/runs/<run-id>` path; arbitrary explicit staging roots теперь участвуют только как output location и не дают ложных `internally inconsistent` конфликтов с explicit `RunId` или canonical `ArtifactsRoot`. Вторая: explicit request теперь проходит pre-validation до override precedence. `-ManifestPath` больше не short-circuit-ит поверх противоречивых explicit inputs (`RunId`, `RunRoot`, `ArtifactsRoot`, `AssemblyBaseDirectory`, `PreferredSourceContextName`): mixed manifest requests fail-fast ещё до выбора resolution branch. Regression contour расширен tests/proofs для noncanonical `RunRoot`, `ManifestPath + RunId`, `ManifestPath + AssemblyBaseDirectory` и manifest+selection конфликтов.

## 2026-04-16 12:12

- Package B для `windows.input` зафиксирован как закрытый runtime-only этап в тогда ещё active exec-plan [docs/exec-plans/completed/completed-2026-04-20-windows-input.md](docs/exec-plans/completed/completed-2026-04-20-windows-input.md): concrete Win32 input slice уже landed без premature public rollout, checklist помечен по факту, а границы ответственности между Package B и следующими Package C/D/E дополнительно выровнены wording-ом. В том же цикле в план явно добавлен residual risk для Package D: observability rollout обязан сохранить distinction между clean dispatch failure и partial side effect, не пытаясь заново выводить ownership/evidence taxonomy из одного top-level результата.

## 2026-04-15 16:32

- Выполнен внутренний maintainability refactor runtime-only Package B для `windows.input` без изменения frozen contract и shipped subset. `Win32InputService` переведён с россыпи mutable loop-переменных на явную execution model (`InputBatchExecutionState` + `InputActionExecutionState`): batch state теперь является source of truth для current action, committed side-effect ownership, factual `resolved_screen_point`/`button`/`targetHwnd`, cancellation observation context и top-level result materialization. Одновременно `InputCancellationMaterializationPolicy` очищена до pure decision по committed context + observation context, helper signatures в `Win32InputService` сжаты вокруг batch state, а legacy `InputCancellationObservationContextSelector` удалён. Regression contour расширен новым `InputBatchExecutionStateTests`, а обязательные `Win32InputServiceTests` / `InputRuntimePolicyTests` и полный `WinBridge.Runtime.Tests` прогон подтверждают, что fail-closed/cancellation/factual-result semantics Package B не ослаблены.

## 2026-04-16 00:40

- Закрыты ещё три review-gap внутри Package B. Во-первых, pointer move теперь моделируется как `committed` и `verified` отдельно: если `SetCursorPos` уже succeeded, а последующий `GetCursorPos` нечитабелен или вернул drift, runtime всё равно сохраняет committed move context и materialize-ит factual failure уже из него, включая refreshed retarget move path. Во-вторых, shared async-state proof больше не требует оба desktop rights сразу для cross-process `GetAsyncKeyState`: probe принимает documented `HOOKCONTROL or JOURNALRECORD` prerequisite и покрыт тестами на оба допустимых ACL-варианта. В-третьих, same-process vs cross-process mode selection теперь централизована в shell-level evaluator и используется и в live input boundary, и в readiness probe path, так что `okno.health` больше не остаётся искусственно pessimistic для same-process foreground case.

## 2026-04-16 01:05

- Shared move seam в `windows.input` доведён до factual observed-point model. `CursorMoveAttemptResult` теперь несёт `ObservedScreenPoint`, если `GetCursorPos` смог прочитать фактическую позицию курсора, и оба caller path — initial move и refreshed retarget move — синхронизируют `InputBatchExecutionState` именно по этой authoritative точке перед committed/failure materialization. Благодаря этому drift-case больше не публикует запрошенную plan point как `resolved_screen_point`, если runtime уже измерил другую фактическую cursor position.

## 2026-04-16 01:22

- `InputBatchExecutionState` и `Win32InputService` дополнительно разведены по линии `planned vs factual point`: `resolved_screen_point` больше не публикуется из одного только `dispatchPlan` до первого pointer side effect. Batch state теперь получает `ResolvedScreenPoint` только из committed/observed evidence (успешный или частично верифицированный move, committed tap paths), поэтому clean pre-move failures — held-input boundary и `SetCursorPos` failure — больше не выглядят как будто factual target point уже существует.

## 2026-04-16 01:36

- Readiness и live dispatch окончательно разведены по async-state semantics. Live `windows.input` по-прежнему использует target-specific same-process shortcut после foreground proof, но `okno.health` снова строит `input` capability из отдельного conservative baseline evaluator без зависимости от текущего foreground owner. Это убирает случайно optimistic `input=ready` при foreground окне самого host-процесса и возвращает `okno.health` к обещанному conservative snapshot contract.

## 2026-04-16 00:20

- В execution model `windows.input` закрыт ещё один factual drift: `InputActionExecutionState` больше не стартует с сырым `action.Button` для click-family action'ов, а получает нормализованный `effectiveButton` до первого возможного committed move. Благодаря этому partial/failure-after-move paths для implicit-left `click`, `double_click` и helper refreshed-move веток теперь materialize-ят тот же `button`, что и успешные results, вместо `null` из raw request payload. Регрессия зафиксирована новыми assertions в `Win32InputServiceTests` и дополнительным invariant test в `InputBatchExecutionStateTests`.

## 2026-04-14 14:35

- Закрыт Package B для `windows.input` без premature public rollout. В `src/WinBridge.Runtime.Windows.Input` появился concrete runtime-only slice: `Win32InputService` с fail-fast batch orchestration, translation-only `InputCoordinateMapper`, target preflight по foreground/minimized/session/integrity и factual per-action results для `move`, `click`, `double_click` и `click(button=right)`, а `Win32InputPlatform` инкапсулирует Win32 probes/dispatch (`SetCursorPos`, `GetCursorPos`, `SendInput`, target process token/session checks). DI в `WinBridge.Runtime` теперь регистрирует `IInputService` / `IInputPlatform`, но `windows.input` остаётся deferred: public MCP handler, `RunGatedAsync(...)`, artifacts/events/materializer rollout и smoke/publication по-прежнему отложены в Package C/D/E. L1 proof обновлён новыми runtime tests (`InputRuntimePolicyTests`, `Win32InputServiceTests`) и полным `WinBridge.Runtime.Tests` contour; дополнительно stale `RuntimeGuardServiceTests` expectation синхронизирован с уже frozen `BuildInput(...)`, где healthy high/uiAccess profile больше не materialize-ится как blocked placeholder.
- После review-гипотез Package B дополнительно укреплён по двум корневым инвариантам pointer runtime. Во-первых, `windows.input` больше не предполагает эксклюзивное владение глобальным cursor/input state по умолчанию: live batch теперь проходит через internal execution gate, поэтому параллельные `move -> verify -> click` цепочки не могут interleave-иться внутри одного процесса. Во-вторых, target admissibility подтянута к самой границе необратимого side effect: `Win32InputService` теперь повторно разрешает live target и заново прогоняет preflight непосредственно перед каждым click dispatch, включая второй tap `double_click`, а если resolved screen point за это время перестал принадлежать текущим live bounds, runtime fail-closed возвращает `point_out_of_bounds` вместо ложного target-bound success. В том же цикле `IInputService` возвращён в internal visibility, так что registration в `AddWinBridgeRuntime()` остаётся internal composition detail, а не accidental public .NET surface.
- Следующая review-итерация закрыла ещё один связанный класс pointer-boundary drift. `Win32InputService` больше не опирается на “голую” `resolvedScreenPoint` при dispatch revalidation: введён immutable `InputPointerDispatchPlan`, а `InputCoordinateMapper` теперь умеет не только строить initial plan, но и повторно валидировать его на dispatch boundary с теми же stale semantics для `capture_pixels`, что использовались при initial map. Одновременно platform boundary больше не отправляет button-only click вслепую: `IInputPlatform.DispatchClick(...)` сначала подтверждает фактическую cursor position непосредственно перед `SendInput`, и только потом выполняет down/up sequence. Это убирает два ложных success-path класса Package B: stale `capture_pixels` clicks после geometry drift и click dispatch после внешнего cursor drift между move и `SendInput`.
- После следующего review-прохода boundary contract доведён до refreshed-plan semantics. `InputCoordinateMapper.TryValidateDispatchPlan(...)` теперь возвращает не только verdict, но и обновлённый authoritative `InputPointerDispatchPlan`, поэтому допустимый `capture_pixels` origin drift на 1px больше не теряется между revalidation и фактическим dispatch: обычный `click`/`click(button=right)` повторно переводят курсор в refreshed point и отчитываются уже о ней, а `double_click` при изменении point между тапами fail-closed завершает gesture вместо silent retarget внутри supposedly same-point double-click contract.
- Ещё одна review-итерация довела gesture policy до explicit split между pre-gesture refresh и irreversible tap boundaries. Для `double_click` refreshed point теперь может появиться только в отдельной pre-gesture phase до первого `TryMoveCursorAndVerify(...)`; после начала gesture обе boundary-проверки работают в режиме `RequireStablePoint`, так что runtime больше не делает скрытый retarget и лишний cursor move внутри первого tap при admissible `capture_pixels` drift. В том же цикле добавлены tests на допустимый 1px refresh до начала `double_click` и на fail-closed path, когда boundary между тапами потребовала бы сменить gesture point.
- Последний review-проход закрыл ещё два boundary blind spot внутри Package B. `InputCoordinateMapper` больше не делает stale-классификацию `capture_pixels` через переполняемые `int`-разности и `Width/Height`: геометрия теперь сравнивается по authoritative edges и derived extents в `long`-домене, поэтому экстремальные bounds не выбрасывают runtime из typed `capture_reference_stale` path и не ослабляют stale predicate из exec-plan. Одновременно `Win32InputPlatform` заменил modifier-only helper на unified ambient input proof: финальный dispatch boundary теперь работает с tri-state (`neutral` / `non-neutral` / `unknown`) snapshot-политикой, доказывает чтение async state с active input desktop, учитывает keyboard modifiers и already-held mouse buttons и fail-closes не только на удерживаемых `Ctrl`/`Shift`/`Alt`/`Win`, но и на held mouse state или непроверяемом ambient input environment. В том же цикле введён единый logical-to-platform mouse button mapper: Package B теперь использует одну и ту же swap-aware semantics layer и для ambient proof, и для Win32 down/up dispatch, так что `button=left/right` остаётся logical contract на системах с swapped buttons, а не расползается между proof/result и физическими `MOUSEEVENTF_*` flags. Новые tests покрывают overflow-safe geometry path, pure ambient-input policy, swapped-button mapping и failure на live `click`/second-tap `double_click`, когда boundary обнаруживает non-neutral или unknown ambient input state.
- Следующая review-итерация добила ещё три contract mismatch на границе Package B. Во-первых, click-first shipped subset теперь валидируется целиком до первого side effect: request-level policy заранее отклоняет `keys[]`, `click(button=middle)` и любые следующие статически известные exclusions, поэтому batch больше не успевает сделать `move`, а потом упасть на позднем unsupported action. Во-вторых, admission/revalidation semantics для explicit target выровнены через shared resolver contract: explicit `hwnd` теперь допускается только если live descriptor уже содержит stable identity, пригодную для последующего `ResolveLiveWindowByIdentity(...)`, так что false `stale_explicit_target` от phase mismatch больше не появляется уже внутри action loop. В-третьих, honest boundary model стала общей и для dispatch outcomes, и для readiness. `Win32InputPlatform` больше не схлопывает partial `SendInput` в чистый failure: для click sequence он различает clean failure и partial dispatch, делает best-effort button-up compensation и возвращает отдельную outcome classification, если side effect уже частично ушёл в систему. А prerequisite "можем честно читать ambient async input state на active input desktop" вынесен в shared probe и используется и в live click boundary, и в `RuntimeGuardPolicy.BuildInput(...)`, поэтому `okno.health` больше не materialize-ит `input=ready` там, где click dispatch гарантированно провалится на final ambient-state proof.
- После следующей review-проверки target lifecycle semantics были дополнительно разведены по capability границам. Предыдущий input-specific fix ошибочно поднял stable-identity gate в общий explicit `HWND` lookup и тем самым усилил shipped `windows.focus_window` / `windows.capture` / attached activate paths. Теперь shared `ResolveExplicitOrAttachedWindow(...)` снова отвечает только за live explicit/attached lookup, а input-specific strictness остаётся локальной для `ResolveInputTarget(...)`, где она действительно нужна из-за последующего `ResolveLiveWindowByIdentity(...)`. Это убирает регрессию для соседних explicit-HWND tools и при этом сохраняет честный fail-closed contract для input runtime, который требует revalidatable stable identity только на тех путях, где есть identity-based next phase.
- Финальная review-итерация Package B дотянула irreversibility boundary до target-level foreground proof. `Win32InputPlatform` больше не редуцирует final foreground snapshot до process-only surrogate: прямо перед `SendInput` runtime подтверждает, что live foreground window всё ещё совпадает с admitted target сначала по `HWND`, а затем и по тому же stable-identity contract (`ProcessId`/`ThreadId`/`ClassName`), который уже использовался на admission/revalidation phase, и только после этого выводит same-process/cross-process async-state readability mode из фактического foreground owner. Это закрывает два ложных success-path класса: focus drift на другое окно того же процесса/activation gap и handle-reuse path, где recycled `HWND` мог бы иначе пройти final boundary как будто это всё ещё тот же target. Новые regression tests в `Win32InputServiceTests` фиксируют fail-closed outcome и на final foreground drift, и на reused-handle foreground replacement перед dispatch.
- Следующая review-итерация довела execution model до единого pointer side-effect contract. Раньше ambient input proof жила только внутри `DispatchClick(...)`, поэтому `SetCursorPos` успевал произойти раньше held-input guard, а standalone `move` вообще обходил его; теперь `Win32InputService.TryMoveCursorAndVerify(...)` перед любым cursor move вызывает общий platform boundary, который заново подтверждает live foreground target и neutral ambient input state до первого `SetCursorPos`. Это убирает ложный fail-open для held mouse buttons/modifiers на `move`, initial click move и refreshed retarget move. В том же цикле cancellation semantics input runtime выровнена с уже существующим factual-after-dispatch паттерном launch/open paths: до первого side effect cancellation по-прежнему выходит исключением, но после любого cursor move или первого tap `double_click` Package B materialize-ит partial `InputResult` с честными `CompletedActionCount` / `FailedActionIndex` / `Actions[]` вместо `OperationCanceledException`, чтобы caller не терял factual batch state и не повторял уже частично выполненный gesture вслепую. Новые regression tests покрывают pre-move held-input rejection и factual cancellation materialization как после completed move action, так и между двумя tap `double_click`.
- Последняя review-итерация закрыла ещё один хвост того же cancellation-класса: раньше factual cancellation policy уже существовала, но observation points оставались слишком редкими, поэтому отмена между `SetCursorPos` и первым tap или сразу после final tap всё ещё могла дать лишний click либо спрятаться за `verify_needed`. Теперь `Win32InputService` использует общий irreversible-phase observer: после каждого pointer side effect runtime немедленно проверяет cancellation перед следующим необратимым шагом, а перед финальным success-return ещё раз переосмысливает уже выполненный batch. Это устраняет окна между move и first click, между tap-ами `double_click` вне `Task.Delay`, и после final dispatch последнего action. Новые regression tests фиксируют cancellation-after-move-without-click и cancellation-after-final-click-dispatch paths.
- Следом была закрыта ещё одна фазовая дыра и один reporting drift в том же семействе. Во-первых, общий cancellation observer теперь привязан не только к явным side effects из основного loop, но и к helper-path retarget move внутри `TryAcceptValidatedDispatchPlan(...)`: если `capture_pixels` refresh требует второй `SetCursorPos`, runtime заново наблюдает cancellation и до этого refreshed move, и сразу после него, поэтому helper больше не может протащить лишний retarget move или click для уже cancelled request. Во-вторых, factual reporting `double_click` переведён с boolean-флага на explicit irreversible-phase state: cancellation после первого tap по-прежнему materialize-ится как “second tap was not executed”, а cancellation после второго tap теперь честно сообщает, что оба tap уже были отправлены. Новые regression tests покрывают cancellation-after-refreshed-move-without-click и truthful cancellation reporting after second tap.
- Последний review-проход довёл cancellation/reporting model до committed-context semantics. Раньше helper cancel path после refreshed move мог терять factual `resolved_screen_point`, а cancel between-actions снова рисковал приписать already-committed side effects следующему action, потому что execution loop хранил только mutable текущий state. Теперь `Win32InputService` ведёт отдельный committed side-effect context с owner action, phase, committed point, button и target hwnd, а cancellation materialization вынесена в отдельную policy. Это даёт два эффекта: helper failure после refreshed `capture_pixels` move сохраняет уже применённый `resolved_screen_point`, и between-actions / final-return cancellation materialize-ятся от имени последнего committed action или вообще без псевдо-failed action, если следующий action ещё не стартовал. Новые regression tests покрывают factual `resolved_screen_point` на helper cancel path, between-actions ownership policy и committed second-tap double-click reporting.
- Следующая review-итерация развела ещё одно смешение в cancellation policy: one committed-context model уже была, но final-return cancellation и between-actions cancellation всё ещё шли через один и тот же factual envelope. Теперь policy получает explicit observation context (`in-flight`, `between-actions`, `after-batch-completed-before-success-return`), поэтому fully committed batch, отменённый только перед финальным `verify_needed`, больше не репортится как будто у него просто “next action was not started”. Это устраняет ложные retry hints для полностью выполненной пачки и завершает переход cancellation/reporting модели с угадывания по `completedActionCount` на явный lifecycle context.
- Поверх этого был закрыт ещё один race-tail между новым `BetweenActions` pre-check и legacy `ThrowIfCancellationRequested()`: раньше cancellation, пришедшая ровно в этом окне, могла всё ещё зайти в `catch` как `InFlightAction` и синтезировать failed pseudo-result для уже completed action set. Теперь `ThrowIfCancellationRequested()` перенесён до сдвига mutable action state, а exception-path context выбирается через отдельный selector, который различает `BetweenActions` и `InFlightAction` по реальному irreversible phase state. Это убирает дублирующий semantic path для одной и той же lifecycle boundary и не даёт race-окну снова размыть factual owner committed side effects.
- Следующая review-итерация закрыла ещё один близкий gap на той же границе: even after the fixed `BetweenActions` pre-check, setup-фаза следующего action всё ещё могла успеть дойти до первого `SetCursorPos`, если cancellation приходила после top-of-loop observer, но до первого move. Теперь перед самым первым `TryMoveCursorAndVerify(...)` введён отдельный `enter action side-effect phase` boundary, который повторно materialize-ит `BetweenActions` cancellation из committed context либо throws only when no committed side effects exist yet. Это делает переход `BetweenActions -> InFlightAction` исполняемым, а не только декларативным, и убирает последний shipped path, где cancelled request ещё мог начать новый cursor move после fully committed предыдущего action.

## 2026-04-13 10:10

- Contract freeze `windows.input` дополнительно сведён к единой action contract model. В `Runtime.Contracts` добавлен `InputActionContractCatalog`, который задаёт allowed/required fields для каждого frozen action literal; `InputRequestValidator` теперь использует эту таблицу для field ownership, поэтому keyboard contracts больше не допускают смешивание `type.text` с `keypress.key`/`repeat`, а explicit `null` чужих полей тоже fail-closed отклоняется по факту присутствия. Manual MCP schema `windows.input` теперь строит `oneOf` action branches из того же catalog, включает required nested fields для `point`, `path`, `captureReference.bounds` и capture geometry, и сохраняет deferred `unsupported` behavior без runtime dispatch.
- MCP schema materialization для `windows.input` теперь различает required/non-null и optional/nullable fields внутри action branches: required `point`, `path`, `coordinateSpace`, `text`, `key`, `delta`, `direction`, `captureReference` и nested `bounds` больше не публикуются как nullable, а optional fields сохраняют nullable surface только там, где validator допускает explicit `null`.
- Action-level optional fields в MCP schema теперь тоже non-null when present: `button`, `keys`, `repeat`, optional `captureReference` и прочие action fields больше не публикуются как nullable, если validator reject-ит explicit `null`; nullable surface сохранён только для nested metadata `effectiveDpi` и `capturedAtUtc`, где validator действительно принимает `null`.
- Scalar constraints `windows.input` вынесены в общий contract layer `InputActionScalarConstraints` и используются одновременно validator-ом и deferred MCP schema: `keypress.key` / `scroll.direction` требуют non-whitespace string, `keypress.repeat` публикуется как `integer >= 1`, `scroll.delta` как non-zero integer, а `captureReference.pixelWidth/pixelHeight` как positive integer. Bounds edge ordering остаётся validator-only constraint, потому что cross-field comparison не выражается честно в текущей JSON Schema без переусложнения.
- Geometry validation для `InputBounds` перестала опираться на potentially overflowing `int` dimensions: validator проверяет edge ordering напрямую (`right > left`, `bottom > top`), а adversarial `int.MinValue`/`int.MaxValue` bounds теперь отклоняются как `invalid_request` до будущего coordinate remap.

## 2026-04-10 18:40

- После второй review-итерации дополнительно ужесточён boundary contract `windows.input` внутри Package A. `InputAction` теперь хранит факт присутствия action-полей отдельно от их содержимого, поэтому validator отклоняет forbidden fields по факту передачи, а не только если строка непустая: pointer/keyboard ветки больше не пропускают `text`, `point`, `button`, `captureReference` и другие чужие поля, переданные как `null`, `""` или whitespace placeholders. Для input binding введена единая request-boundary модель: `InputRequest`, `InputAction`, input-specific `InputPoint`, `InputBounds` и `InputCaptureReference` больше не отдают malformed `null`, invalid scalar/list tokens или целиком неверные token types на откуп `System.Text.Json`, explicit zero coordinates по-прежнему валидны, а malformed `actions`, action objects, `keys`, `point`, `path`, `captureReference`, `bounds`, `pixelWidth` и `pixelHeight` теперь consistently materialize-ятся как typed `invalid_request`. Deferred public `windows.input` переведён на schema-preserving manual registration, чтобы замороженный `actions[]` MCP envelope оставался честным и не зависел от reflection schema degradation из-за converters.
- Source-of-truth docs дополнительно разведены по слоям: `docs/product/okno-spec.md` и `docs/exec-plans/completed/completed-2026-04-20-windows-input.md` теперь явно отличают frozen Package A action set (`move` / `click` / `double_click` / `drag` / `scroll` / `type` / `keypress`) от later extensions (`hotkey`, `paste`), а clipboard/product wording больше не обещает `paste` как уже входящий в текущий freeze `windows.input`.

## 2026-04-10 17:39

- После review-hypotheses дополнительно усилен boundary contract `windows.input` без выхода за Package B/C/D/E. В `InputAction` разделены token-like и payload-like поля: `text` больше не теряет leading/trailing whitespace на DTO boundary, а validator перестал сводить whitespace-only text к `invalid_request`. В том же цикле `InputRequestValidator` получил structural preflight для nested collections: `actions[]` и `drag.path[]` теперь fail-closed materialize-ят typed `invalid_request` для `null`-элементов вместо `NullReferenceException`, а capture-reference checks дополнительно страхуют nested object shape до runtime dispatch.
- Закрыт contract propagation drift между новым input readiness baseline и downstream verification/publication surface. `AdminToolTests` и default fake health snapshot синхронизированы с реальным `BuildInput(...)`: medium-integrity profile без `uiAccess` теперь ожидается как `input=degraded` с warning `input_uipi_barrier_present`, а не как permanently blocked placeholder. Одновременно deferred public MCP surface `windows.input` выровнен с frozen Package A contract: reflection-published tool больше не advertises legacy `actionsJson`, а использует `actions`/`hwnd`/`confirm`; это подтверждено isolated integration build + `tools/list`/deferred-call verification через временный server output без затрагивания locked primary `src/WinBridge.Server/bin`.

## 2026-04-10 16:33

- Для `windows.input` закрыт Package A contract freeze без premature publication: в `src/WinBridge.Runtime.Contracts` появился новый `Input*` contract family как source of truth для ordered batch input, value sets, validator architecture, result/status/failure literals и strict input target resolution; shared guard layer больше не держит `input` в always-blocked placeholder и теперь честно различает `ready/degraded/blocked/unknown`; `IWindowTargetResolver` получил отдельный `ResolveInputTarget(...)` без active fallback, а product/interop docs, tooling manifest и active exec-plan синхронизированы под решения `one tool`, `click(button=right)`, `capture_pixels + screen`, `verify_needed` и `supports_dry_run=false`, при этом `windows.input` остаётся deferred, public handler/runtime dispatch/smoke не включались.

## 2026-04-10 13:32

- Проведён независимый repo audit по feedback-замечаниям после `windows.open_target` и приняты только подтверждённые вещи. В roadmap добавлен отдельный planned slice `windows.region_capture` как узкий visual follow-up к уже shipped capture family: reference repos (`Peekaboo`, `Windows-MCP`) и текущие docs `okno-vision` / `observe-capture` подтвердили, что region-focused visual proof полезен для verify-after-action и future OCR fallback, но его не нужно путать с broad OCR/browser subsystem. В том же цикле `okno-roadmap`, `okno-spec` и `observe-capture` синхронизированы с этой более узкой product framing, а ближайший next feature не изменён: первым по-прежнему остаётся `windows.input` (`click` first).
- Policy и verification контур усилены по двум реальным operational gaps. Во-первых, plugin install surface больше не считается закрытым только repo-side правками: `AGENTS.md` и exec-plan [docs/exec-plans/completed/completed-2026-04-10-okno-plugin-install-surface.md](docs/exec-plans/completed/completed-2026-04-10-okno-plugin-install-surface.md) теперь явно требуют cache-installed copy proof, restart Codex/new thread и минимум один read-only tool call (`okno.health` или `okno.contract`) из fresh-thread materialization path. Во-вторых, honest deferred behavior поднят из архитектурного принципа в механический integration gate: `layers.md` и repo guardrails фиксируют rule “не публикуется либо честно отвечает `unsupported/deferred`”, а `McpProtocolSmokeTests` теперь проверяет invocation contract для всех текущих deferred public tools без generic transport error. Для docs sync обновлён generator `scripts/refresh-generated-docs.ps1`, чтобы `test-matrix` больше не утверждал, будто deferred-tool contract tests отсутствуют.
- Исправлен repo-side install surface для Codex plugin `okno`: root-cause оказался не только в stale home cache, но и в том, что plugin launcher внутри репозитория был корректен лишь для source checkout и не умел честно разрешать repo root из cache-installed copy. Теперь plugin root содержит resolver `plugins/okno/resolve-okno-repo-root.ps1`, launcher `plugins/okno/run-okno-mcp.ps1` сначала пытается найти repo по ancestor search, а для install cache принимает explicit repo-root hint через untracked `plugins/okno/.okno-repo-root.txt` или `OKNO_REPO_ROOT`.
- В repo control plane добавлен helper `scripts/codex/write-okno-plugin-repo-root-hint.ps1`, который materialize-ит machine-local repo-root hint в source plugin без возврата к tracked absolute paths в `.mcp.json`; `.gitignore`, `README.md`, `plugins/okno/README.md` и exec-plan [docs/exec-plans/completed/completed-2026-04-10-okno-plugin-install-surface.md](docs/exec-plans/completed/completed-2026-04-10-okno-plugin-install-surface.md) синхронизированы с этой моделью.
- Generated command inventory теперь явно включает repo-side prepare step для plugin install surface, чтобы следующий инженер видел границу ответственности: внутри репозитория source-of-truth уже готовит install-safe launcher contract, а снаружи остаются только refresh/reinstall cache copy plugin и restart Codex/new thread.

## 2026-04-10 12:18

- После shipped `windows.open_target` в roadmap добавлен отдельный planned slice `windows.surface_lifecycle`: follow-up нужен не для самого shell-open, а для безопасного claim/reconcile/close только owned shell/window/dialog surfaces после `launch_process` / `open_target`. Решение зафиксировано отдельно, а не как hidden расширение `windows.open_target`, потому что reused Explorer/browser window or tab не даёт ownership автоматически и cleanup здесь должен оставаться fail-closed.
- Product spec [docs/product/okno-spec.md](docs/product/okno-spec.md) дополнительно закрепил эту границу: `windows.open_target` не включает hidden teardown/cleanup, а safe close остаётся задачей будущего ownership/lifecycle slice.
- В архитектурный контур добавлена policy [docs/architecture/reference-research-policy.md](docs/architecture/reference-research-policy.md): она фиксирует, как использовать локальный `references/` cache и внешние reference repos без подмены source of truth проекта, и явно задаёт иерархию `Okno docs -> official Microsoft/MCP/OpenAI docs -> reference repos -> локальные эксперименты`. В том же цикле `AGENTS.md` и architecture index синхронизированы с новой policy и с локальным, но git-ignored, reference cache.
- Проведён полный audit [docs/product/okno-roadmap.md](docs/product/okno-roadmap.md) по фактическому repo state, локальным reference repos и OpenAI/MCP alignment: документ перестал быть длинным greenfield/V1 stage-by-stage narrative и теперь играет роль живого capability map. Таблица пересобрана вокруг реальных repo/tool slices, статусы и проценты выровнены под shipped `windows.launch_process` / `windows.open_target`, текущий порядок поставки смещён к `windows.input(click first)`, а OpenAI/Codex alignment вынесен как отдельный product-facing критерий вместо вторичного комментария.
- Проведён repo-wide product language audit по живому surface: из README, AGENTS, product docs, architecture docs, user-facing tool descriptions, validator reason strings и generated docs убрана `V1/MVP` семантика там, где проект уже должен описываться как зрелый рабочий продукт с shipped slices. Исторические completed exec-plans и старые changelog entries не переписывались намеренно, чтобы не ломать историю решений, а generated surface после правок синхронизирован повторным `scripts/refresh-generated-docs.ps1`.

## 2026-04-10 11:37

- Был поставлен workstream [docs/exec-plans/completed/completed-2026-04-20-windows-input.md](docs/exec-plans/completed/completed-2026-04-20-windows-input.md) для `windows.input`: вместо расплывчатого “сделать input вообще” зафиксирован узкий delivery order `click first + action schema freeze` и explicit contract, по которому следующий инженер может идти сразу в реализацию. План сразу привязан к уже shipped repo contour `readiness -> ToolExecutionGate -> RunGated -> runtime service -> evidence -> smoke/docs`, а не к isolated `SendInput` helper.
- В том же exec-plan заранее закрыты ключевые forks, которые иначе размыли бы slice позже: выбран один quiet tool `windows.input` с `actions[]`, canonical public coordinate space = `capture_pixels`, `right_click` сведён к `click(button=right)`, hidden attach/focus/activate запрещены, click-first live path нормализован вокруг `verify_needed` + explicit `windows.wait`, а shared `BuildInput(...)` обозначен как обязательный refactor из current deferred-blocked placeholder в real readiness baseline до public publication.
- После review-прохода этот же active plan дополнительно ужесточён на contract wording уровне: для `capture_reference_stale` зафиксирован точный stale predicate с `1px` tolerance только для origin rounding drift, для `double_click` заморожена системно-совместимая timing/distance semantics через `GetDoubleClickTime` и `SM_CX/CYDOUBLECLK`, batch execution получил явные non-transactional rules c `FailedActionIndex`, а `Package E` и L3 acceptance теперь требуют fresh-host/new-thread publication proof после restart, а не только warm-process smoke.

## 2026-04-09 14:52

- После повторного полного proof-pass exec-plan [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) архивирован из `docs/exec-plans/active` в `docs/exec-plans/completed`, чтобы lifecycle снова оставался консистентным: `windows.open_target` уже закрыт end-to-end, поэтому в `active` больше не должен висеть как живой workstream. В том же цикле проверен актуальный branch state через `scripts/codex/verify.ps1`, который остаётся зелёным после smoke/docs rollout и package-level hardening.
- Post-completion review hardening по той же ветке `windows.open_target` закрыл ещё четыре подтверждённых класса дефектов без расширения public V1 surface: exceptional live-path после allowed preflight больше не обходит `OpenTargetResultMaterializer` и всегда получает typed terminal-failure artifact/event trail с `failure_stage`, explicit document safety policy теперь fail-closed блокирует Python launcher-associated `.py/.pyw/.pyc` под `targetKind=document`, Win32 shell normalizer распознаёт `GetLastError(ERROR_NO_ASSOCIATION)` вместе с legacy `SE_ERR_NOASSOC`, а dedicated STA executor перестал игнорировать caller cancellation на стадии wait-side blocking. В том же цикле completed exec-plan обновлён factual note-ами про этот internal hardening, а узкий runtime verification contour по `OpenTarget*` tests снова пройден зелёным.

## 2026-04-09 16:06

- В том же review-hardening цикле стабилизирован локальный verification control plane: `scripts/ci.ps1` больше не исполняет `bootstrap/build/test/smoke/refresh-generated-docs` в одной общей PowerShell-сессии, а запускает каждый top-level step в fresh `powershell -ExecutionPolicy Bypass -File ...` процессе через shared helper в `scripts/common.ps1`. Причина была orchestration-level, а не продуктовой: standalone `scripts/smoke.ps1` проходил зелёно, но combined `scripts/codex/verify.ps1` ловил session-contamination flake на `windows.wait(visual_changed)` после тестового шага. После process-isolation fix полный sequential contour снова подтверждён зелёным через `scripts/codex/verify.ps1`.
- Следующая review-итерация по той же ветке `windows.open_target` закрыла ещё три связанных класса дефектов без расширения public surface: dedicated STA executor теперь различает `pre-dispatch cancellation` и `cancellation after dispatch`, поэтому отмена больше не может одновременно выглядеть как чистый `OperationCanceledException` и допускать неучтённый shell side effect; live path inspector стал fast local-only seam и деградирует в `Unresolved` для UNC/remote/device-style paths вместо token-blind сетевого preflight; document safety policy расширена до packaged interpreter families `.pyz/.pyzw` и Java executable `jar`, а не только до plain Python scripts. В том же цикле helper `Invoke-ScriptProcessStep` ужесточён до `powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File ...`, чтобы child-process isolation в `ci.ps1` не зависела от пользовательских PowerShell profiles. Узкие red/green tests для executor, path inspector и document policy пройдены зелёным, open-target runtime/integration contour остаётся зелёным, а полный `scripts/codex/verify.ps1` снова подтверждён.
- Финальный follow-up в той же review-ветке убрал regression drift в `OpenTargetPathInspector`: fast local-only refinement больше не трактуется как `fixed-only`, а сохраняет truthful file-vs-directory refinement для supported local media classes `DRIVE_FIXED`, `DRIVE_REMOVABLE`, `DRIVE_CDROM` и `DRIVE_RAMDISK`, оставляя деградацию в `Unresolved` только для UNC и `DRIVE_REMOTE`. В том же цикле completed exec-plan явно зафиксировал текущую intentional границу cancellation semantics: после необратимого shell dispatch single-result V1 ждёт factual completion и не рекламирует bounded caller-side cancel без отдельной task/pending модели. Новые path-inspector tests и повторный полный `scripts/codex/verify.ps1` подтверждены зелёным.
- Validator-focused follow-up в том же `windows.open_target` hardening цикле восстановил точную path admission boundary для `document` и `folder`: contract уже обещал только absolute DOS local и UNC paths, но classifier допускал любые `Path.IsPathFullyQualified(...)` формы, включая device-style `\\?\`, `\\.\` и `\\?\UNC\...`. Для закрытия этого drift admission boundary вынесен в explicit `OpenTargetPathAdmissionPolicy`, который различает DOS local, UNC, device-style, drive-relative, relative и URI classes и reject-ит device-style forms как `invalid_request` ещё до shell-open path. Regression matrix расширена targeted tests на `\\?\`, `\\.\` и `\\?\UNC`, completed exec-plan синхронизирован с этой более точной V1 границей, а повторный `scripts/codex/verify.ps1` остаётся зелёным.

## 2026-04-09 13:48

- Review-driven follow-up внутри уже закрытого `Package C` для [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) ужесточил smoke-harness инвариант для unowned shell-open side effects без изменения shipped public contract `windows.open_target`. Проблема была не в самом tool contract, а в порядке proof/cleanup: live `folder`-open запускался слишком рано в `scripts/smoke.ps1`, до завершения owned helper-based contour, и затем пытался делать best-effort `Remove-Item` по probe-folder, хотя ownership Explorer/process/window для folder-open доказать нельзя. Follow-up довёл этот инвариант до конца: `windows.open_target` live proof остаётся terminal-stage после всех owned helper checks, но disposable probe-target теперь создаётся во внешнем temp-root вне repo-owned `artifacts/smoke/<run_id>`, а не внутри evidence tree. Это убрало второй симптом того же класса: even when shell handler держит folder open, pinned остаётся внешний probe-target, а не сам smoke run directory. Параллельно smoke/docs narrative вынесен в shared `scripts/common.ps1`, чтобы `scripts/refresh-generated-docs.ps1` и generated `test-matrix` больше не расходились с реальным terminal stage order. В том же цикле повторно пройдены `scripts/smoke.ps1` и `scripts/codex/verify.ps1`.

## 2026-04-09 13:21

- Закрыт `Package C` в active workstream [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md): `windows.open_target` теперь доказан end-to-end как shipped-ready launch-family tool без изменения frozen V1 contract из `Package A/B`. `scripts/smoke.ps1` получил отдельный L3 story на deterministic `folder` target: dry-run path доказывает safe preview + internal marker `open_target.preview.completed` без runtime artifact/event, live path доказывает `done` c `target_open_requested | handler_process_observed`, обязательным `acceptedAtUtc`, canonical `launch/open-target-*.json` и safe parity с `open_target.runtime.completed`, при этом smoke не требует нового PID/нового окна и не зависит от browser/editor-specific handler. В том же цикле `windows.open_target` честно переведён в `SmokeRequired`, generator/source docs синхронизированы с новым smoke story, roadmap row 14 поднят до `реализовано`, product spec получил компактный frozen contract, а полный sequential contour `scripts/build.ps1 -> scripts/test.ps1 -> scripts/smoke.ps1 -> scripts/refresh-generated-docs.ps1 -> scripts/codex/verify.ps1` пройден зелёным. Единственный технический blocker-fix в этом пакете был верификационный, а не продуктовый: перед финальным contour пришлось один раз синхронизировать committed generated `project-interfaces.*` с уже изменённым manifest, потому что exporter tests пинят текущий contract snapshot до `scripts/test.ps1`.

## 2026-04-09 10:56

- Review-driven hardening внутри уже закрытого `Package B` для [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) восстановил два инварианта shipped `windows.open_target` без захода в `Package C`: любой factual live outcome после preflight теперь проходит через единый `_resultMaterializer.Materialize(...)`, поэтому kind-mismatch ветки `document -> directory` и `folder -> file` больше не обходят `open_target.runtime.completed` и `launch/open-target-*.json`, а boundary catch-all перестал downcast-ить unexpected internal failure в доменный `shell_rejected_target` и отдаёт sanitised `failed` без ложного shell-level `failureCode`. В том же цикле manifest publication больше не помечает `windows.open_target` как `SmokeRequired` до появления реального smoke scenario из `Package C`, а red/green tests отдельно закрепили live kind-mismatch materialization, отсутствие semantic downcast для unexpected boundary failure и sync между implemented/publication surface и canonical smoke-required list.

## 2026-04-08 18:16

- Закрыт `Package B` в active workstream [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) без захода в `Package C`: `windows.open_target` переведён в public implemented surface через `ToolContractManifest`, `okno.contract` и фактический `tools/list`, для него добавлены programmatic MCP registration и raw-arguments gated boundary в `WindowTools`, а runtime получил отдельный launch-family observability path с `open_target.runtime.completed`, internal marker `open_target.preview.completed` и JSON artifacts `artifacts/diagnostics/<run_id>/launch/open-target-*.json` без новой redaction class и без изменения frozen request/result semantics из `Package A`. В том же цикле узкие runtime/integration/MCP tests подтвердили best-effort observability, tool-level failed paths для invalid binding и сохранение split с уже shipped `windows.launch_process`.

## 2026-04-08 14:44

- Review-driven hardening внутри того же `Package A` для [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) довёл foundation до трёх согласованных инвариантов без захода в `Package B/C`. Во-первых, preflight classifier снова стал pure и deterministic: он больше не делает filesystem probe для path-based targets и ограничивается contract validation, safe identity и централизованной launcher-like policy, куда дополнительно включён ClickOnce deployment manifest `.application`. Во-вторых, live semantic refinement для `document`/`folder` переехал в отдельный runtime path inspector внутри `OpenTargetService`, поэтому mismatched existing file-vs-folder cases теперь блокируются только в live execution phase, а blocked/dry-run preview path не зависит от UNC/network/filesystem state. В-третьих, canonical `ShellExecuteExW` invocation снова выражает системную default shell semantics через `lpVerb = NULL`, а не через насильный literal `open`, что сохраняет заявленный V1 contract “default open action” без public `verb`. Follow-up в том же цикле обновил regression tests на ClickOnce/launcher policy, default-verb invocation и pure-preflight vs live-resolution split, после чего узкий verification contour (`OpenTarget*` runtime tests, `ToolContractManifestTests`, `AdminToolTests`) снова пройден зелёным.

## 2026-04-08 13:48

- Закрыт `Package A` в active workstream [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) без захода в public publication, boundary handlers и observability rollout: в runtime добавлен отдельный sibling-срез для `windows.open_target`, который фиксирует узкий V1 contract (`document` / `folder` / `url(http/https)`), internal-only `FutureOpenTargetDescriptor`, fail-closed validator/classifier и dedicated shell-open foundation поверх `ShellExecuteExW` с factual success semantics `target_open_requested` / `handler_process_observed`. В том же цикле `WinBridge.Runtime.Windows.Launch` получил `IOpenTargetService`, `IOpenTargetPlatform`, `OpenTargetService` и `ShellExecuteOpenTargetPlatform` с STA+COM execution path, optional `HandlerProcessId` только как enrichment и без artifacts/runtime events, а узкие runtime/integration tests отдельно зафиксировали contract freeze, ShellExecute failure mapping, DI registration и отсутствие premature publication в `okno.health` / `okno.contract`.

## 2026-04-08 11:53

- Active exec-plan [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) перегруппирован из пяти мелких delivery packages в три более крупных workstream-а без сокращения scope: теперь `Package A` объединяет contract freeze и runtime shell-open foundation, `Package B` собирает public MCP boundary вместе с observability/redaction rollout, а `Package C` закрывает verification/smoke/docs acceptance. Это не меняет V1 contract decisions и не убирает ни один integration point, test requirement или docs-sync шаг, но делает ownership и execution order более читаемыми для полной реализации.

## 2026-04-08 11:20

- Добавлен новый active exec-plan [docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md](docs/exec-plans/completed/completed-2026-04-09-windows-open-target.md) для следующего shipped launch-family slice после `windows.launch_process`: документ фиксирует `windows.open_target` как отдельный shell-open tool для `document` / `folder` / `url(http/https)` без смешения с direct process launch, удерживает reuse уже shipped foundation (`RuntimeGuardPolicy.BuildLaunch(...)`, shared `RunGated(...)`, `launch_payload`, launch-family artifact/event pattern) и закрывает первые design forks до начала реализации. В плане заранее зафиксированы узкие V1 границы: только default open action без public `verb`, без `workingDirectory`, без `mailto` и custom URI schemes, success определяется как shell acceptance (`target_open_requested`) с optional `handler_process_observed`, а не через обязательный PID/window contract. Отдельно зафиксированы file-level integration points, отдельный `ShellExecuteExW`-oriented runtime seam в `WinBridge.Runtime.Windows.Launch`, launch-family evidence contract с dedicated `open_target.runtime.completed` и `launch/open-target-*.json`, L1/L2/L3 test ladder и недеструктивная smoke strategy на folder target без browser/editor assumptions.

## 2026-04-08 08:45

- После перевода `windows.launch_process` workstream в completed lifecycle финальный `scripts/codex/verify.ps1` вскрыл branch-local недетерминизм в `PollingWaitServiceTests.WaitAsyncBoundsUiAutomationProbeByRemainingTimeout`: тест использовал слишком маленький `TimeoutMs=20` и иногда завершался до первого probe на загруженной машине, хотя сам shipped runtime contract не менялся. Тестовая проверка widened до устойчивого бюджета `80ms`, чтобы она продолжала доказывать bounded propagation оставшегося timeout в UIA probe, но больше не зависела от планировщика и локальной нагрузки. После этого полный final contour `scripts/codex/verify.ps1` снова пройден зелёным на ветке `codex/windows-launch-process`, так что фича и lifecycle/docs состояние подтверждены уже без условностей и ручных оговорок про случайный verify drift.

## 2026-04-08 09:31

- Follow-up после external review закрыл структурную коллизию между public payload metadata и internal shared-gate metadata в `tool.invocation.completed`: `AuditLog` больше не пишет внутренние decision markers в те же flat keys, что и payload-shaped поля результата, а namespac-ит их как `gate_decision`, `gate_risk_level`, `gate_guard_capability`, `gate_requires_confirmation`, `gate_dry_run_supported`, `gate_reason_codes`. Это восстановило основной shipped-invariant для `windows.launch_process`: validation-only failures, dry-run preview и allowed-path runtime failures теперь сохраняют public `decision` в completion audit trail и одновременно не теряют внутренний gate verdict. В том же цикле обновлены observability docs, добавлен unit test на collision-класс в audit layer, расширены launch/tool-execution boundary tests и повторно пройден полный `scripts/codex/verify.ps1`.

## 2026-04-08 08:40

- После финальной verification wave exec-plan [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md) архивирован из `active` в `completed`, чтобы lifecycle `docs/exec-plans` снова оставался консистентным: в `active` остаются только живые workstream-ы, а полностью завершённый `windows.launch_process` хранится рядом с остальными completed plans.

## 2026-04-06 18:08

- Закрыт `Package E` в active workstream [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md): shipped rollout `windows.launch_process` теперь доказан end-to-end через реальный L3 smoke story, где `scripts/smoke.ps1` сначала делает safe dry-run preview, затем live helper launch через сам tool, cross-check-ит `processId` / `mainWindowHandle`, canonical `launch/<launch_id>.json`, полный safe parity set `launch.runtime.completed` и только потом переиспользует то же окно в attach/UIA/capture/wait части сценария. В том же цикле smoke получил explicit negative proof для dry-run path: preview-only branch теперь фиксируется internal event `launch.preview.completed`, factual runtime по-прежнему доказывается отсутствием `launch.runtime.completed`, а cleanup dry-run/live leaks больше не зависит от MCP transport и не special-case-ится на непустой baseline. Follow-up hardening усилил сам smoke harness на трёх осмысленных местах: live audit-marker polling теперь читает `events.jsonl` только до последней завершённой строки, чтобы append-in-progress не давал ложные JSON parse failures; ownership helper-а переведён с user-visible title на hidden run-scoped marker `--smoke-run-id`, чтобы cleanup не путал текущий smoke run с чужими helper-процессами; а fallback reconciliation теперь строится как OS-only bounded protocol, который сразу после dry-run/live tool call сохраняет marker-scoped ownership и ранний candidate snapshot по exact helper `ExecutablePath`, а в proof/cleanup фазе крутит единый resolution loop: на каждой итерации повторно открывает candidate processes и уже на текущем candidate set boundedly проверяет `MainWindowTitle`. Этот loop использует тот же helper window materialization budget, что и основной launch proof, поэтому orphan cleanup не требует follow-up `windows.list_windows` roundtrip даже при marker-loss, delayed process discovery, delayed title materialization и transport failure. Review-driven follow-up выровнял сам diagnostics layer: для runtime/proof markers добавлен единый `AuditLog.TryRecordRuntimeEvent(...)`, на него переведены launch/wait/uia runtime event writers и оба новых internal proof markers, так что observability write снова остаётся best-effort side effect и не меняет factual result ни для dry-run preview, ни для `visual_changed`. `docs/architecture/observability.md` теперь тоже явно разделяет public runtime events и internal proof markers, включая best-effort wording для `launch`, `wait` и `uia` investigation flow. Deterministic docs/export drift закрыт на source-of-truth уровне через `ToolContractExporter`, exporter tests и generated docs (`project-interfaces`, `commands`, `test-matrix`), product docs синхронизированы с shipped state (`okno-roadmap` row 13 -> `реализовано` / `85%`, `okno-spec` получил компактный контракт `windows.launch_process` и explicit best-effort wording для `artifactPath`), а completed exec-plan заархивирован рядом с остальными завершёнными workstream-ами. По пути пришлось сделать только минимальные blocker-fixes вокруг smoke harness и verification UX, не меняя public launch contract: visual helper переведён с endless heartbeat на bounded burst с explicit smoke-provided budget, helper arm теперь синхронизируется с real baseline-ready marker `wait.visual.baseline_captured` вместо arm-delay эвристики, helper lifetime по-прежнему передаётся явно из smoke и страхуется self-close timer-ом, foreground/focus waits в smoke стали менее flaky за счёт более устойчивых timeout budgets, а `PollingWaitService` coverage осталась сфокусированной на релевантных shipped contracts — bounded timeout plumbing, visual baseline runtime marker и process-isolated worker timeout — без branch-local теста, который нормализовал бы wait дольше публичного `timeoutMs`. Финальный sequential contour `scripts/build.ps1 -> scripts/test.ps1 -> scripts/smoke.ps1 -> scripts/refresh-generated-docs.ps1 -> scripts/codex/verify.ps1` пройден зелёным.

## 2026-04-06 11:20

- Закрыт `Package C` в active workstream [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md) без захода в evidence/artifact/smoke rollout: `windows.launch_process` переведён из internal contract freeze в реальный public MCP tool, `WindowTools` получил gated boundary только через `RunGatedAsync(...)`, а publication surface синхронизирован между `ToolContractManifest.All`, implemented/exported `okno.contract` и фактическим `tools/list`. В том же цикле boundary начал materialize-ить canonical payloads для `blocked`, `needs_confirmation`, `dry_run_only`, allowed dry-run preview и allowed live runtime path через `IProcessLaunchService`, причём semantic handoff для allowed-path failure закрыт явно: если shared gate уже дал `allowed`, но дальше boundary/runtime возвращает failure, public payload публикует `Decision=failed`, а исходный gate verdict остаётся только во внутреннем audit metadata. Follow-up внутри того же пакета убрал остаточный transport drift полностью: `windows.launch_process` больше не использует attribute/scalar auto-binding и зарегистрирован как programmatic MCP tool с explicit flat `inputSchema`, а canonical `LaunchProcessRequest` теперь всегда bind-ится из raw MCP `arguments`. Благодаря этому `environment`, произвольные extra fields, missing required `executable` и type-mismatch в published полях снова materialize-ятся как tool-level `failed` result вместо generic SDK error или ложного `needs_confirmation`. Узкие runtime/integration tests закрепили manifest/export/admin-visible sync, synthetic launch boundary coverage и реальный `tools/call` reject-path для extra transport fields и malformed published fields, но `launch.runtime.completed`, launch artifacts, smoke story и broad docs rollout по-прежнему остаются в следующих пакетах.

## 2026-04-06 13:50

- Закрыт `Package D` в active workstream [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md) без захода в smoke и broad product/docs rollout: `windows.launch_process` получил dedicated observability слой поверх уже shipped runtime/boundary semantics, причём factual source of truth остался один и тот же `LaunchProcessResult`. В `WinBridge.Runtime.Windows.Launch` добавлены `launch` artifact writer/materializer и runtime event `launch.runtime.completed`, поэтому каждый factual live runtime result теперь materialize-ит `artifacts/diagnostics/<run_id>/launch/launch-<timestamp>-<nonce>.json` и согласованный runtime event с safe metadata (`status`, `decision`, `resultMode`, `failureCode`, `executableIdentity`, `processId`, `startedAtUtc`, `hasExited`, `exitCode`, `mainWindowObserved`, `mainWindowHandle`, `mainWindowObservationStatus`, `artifactPath`, optional `failureStage` / `exceptionType`). Follow-up в том же пакете довёл redaction-first path до launch runtime evidence: existing `launch_payload` reuse-ится и для tool completion, и для `launch.runtime.completed`, `executable_identity` дополнительно normalizes-ится до basename, `working_directory`/`arguments` не попадают в audit event-data, а failure summary для launch runtime event больше не может утекать через `summary.md`. Для `artifact_write` failure выбран explicit fail-safe contract без semantic regression: public/result status не downcast-ится, artifact path остаётся `null`, runtime event несёт только `failure_stage=artifact_write` и `exception_type`, а launch artifact не пишет raw `exception_message`. Узкие L1/L2 tests закрепили runtime artifact/event alignment и completion-audit linkage по `artifact_path`, а docs sync в рамках пакета ограничен observability/generated interfaces/active exec-plan/changelog; smoke, roadmap/spec и full verification contour по-прежнему остаются на `Package E`.
- Follow-up hardening внутри того же `Package D` закрыл три точечных contract drift-а, найденных review pass: validation-only failures больше не materialize-ят launch artifact/event до входа в factual runtime path, runtime event write стал best-effort side effect и больше не может превратить уже состоявшийся launch в generic boundary failure, а `summary.md` теперь действительно несёт safe launch identifiers (`executableIdentity`, `processId`, `resultMode`, `artifactPath`) вместо wholly generic runtime message. Заодно strategy записи launch artifact переведена на staged temp-file publish с cleanup temp-path на failure, чтобы `artifact_write` не оставлял partially-published final artifact.
- Ещё один follow-up в том же `Package D` довёл эту policy до shipped public path целиком: `WindowTools` больше не считает completion audit обязательной semantic частью already-computed live launch result, поэтому поздний сбой `tool.invocation.completed` не может переписать factual `windows.launch_process` payload в generic failure после уже состоявшегося side effect. Это закреплено отдельным L2 integration test на blocked audit file после возврата runtime result.

## 2026-04-01 12:42

- Закрыт `Package B` в active workstream [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md) без захода в handler/publication/evidence rollout: добавлен отдельный capability project `WinBridge.Runtime.Windows.Launch` с публичным seam `IProcessLaunchService`, а runtime теперь умеет честно materialize-ить live `LaunchProcessResult` поверх frozen request contract из `Package A`. Direct launch path строится только через `ProcessStartInfo` с `UseShellExecute = false` и `ArgumentList`, optional `waitForWindow` реализован как runtime-only observation step через `WaitForInputIdle`, `Refresh` и `MainWindowHandle` без auto-focus, auto-attach и без premature public MCP publication. Follow-up hardening в том же пакете убрал lossy `File.Exists` / `Directory.Exists` preflight в пользу authoritative `Process.Start(...)` failure mapping, сделал `WaitForInputIdle` bounded/cancel-aware через короткие slices вместо одного долгого blocking wait и довёл time abstraction до конца через provider-aware observation delay, выровнял process-state snapshot между started/window observation ветками, включая cancel-path после старта, синхронизировал safe executable identity с validator-классификацией, чтобы rejected URI больше не тащили query/fragment в failure result, убрал ложноположительный `main_window_observation_not_supported`, развёл `input-idle timeout` от финального window verdict и принудил final deadline snapshot идти через свежий `Refresh()`-aware observation path. В результате `WaitForInputIdle(...)=false` теперь считается только промежуточным readiness-signal, late-after-deadline window больше не даёт ложный `window_observed`, exited snapshot больше не может маскироваться под `window_observed`, ambiguous `Win32Exception(3)` больше не сводится ни к `executable_not_found`, ни к `working_directory_not_found` без достаточного сигнала, а started-result ветки снимают fresh factual snapshot без legacy cached-state assumptions. В том же цикле `AddWinBridgeRuntime(...)` получил DI registration для нового launch service, active exec-plan checklist выровнен под фактически закрытые пункты `Package B`, а узкие L1 runtime tests закрепили direct start info semantics, `process_started` / `process_started_and_exited` / `window_observed` mapping и failure branches `process_exited_before_window`, `main_window_timeout` и `main_window_not_observed`.

## 2026-03-31 18:55

- Добавлен новый active exec-plan [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md) для следующего shipped action slice после safety baseline: документ фиксирует `windows.launch_process` как отдельный explicit executable/process launch tool, удерживает split с `windows.open_target`, переиспользует existing `RunGated(...)` / launch-readiness / `launch_payload` foundation и задаёт file-level integration map, official constraints, public DTO proposal, evidence/redaction contract, delivery packages, test ladder и smoke story без focus-dependent acceptance.

## 2026-03-31 18:32

- Закрыт `Package A` в active workstream [docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md](docs/exec-plans/completed/completed-2026-04-08-windows-launch-process.md) без захода в runtime/service/handler rollout: в `WinBridge.Runtime.Contracts` добавлен frozen DTO/value-set/validator seam для `windows.launch_process`, а в `WinBridge.Runtime.Tooling` зафиксирован internal-only `FutureLaunchProcessDescriptor` с canonical summary, parameter wording и final execution-policy metadata. Поверх этого boundary request contract доведён до canonical shape, чтобы следующий пакет не латал семантику задним числом: `timeoutMs` теперь distinguishable optional override, `args` получили canonical empty default и reject null-elements, request boundary fail-closed сохраняет и отклоняет любые unsupported extra fields, rooted directory/document/shell-open targets и drive-relative paths больше не проходят как launchable executable intent, а preview/result явным именованием фиксируют safe executable identity вместо raw path. При этом `windows.launch_process` намеренно не переведён в `ToolContractManifest.All`, `okno.contract`, `okno.health`, exporter и фактический `tools/list`, чтобы current shipped surface оставался честно синхронизирован с отсутствующим handler boundary. В том же цикле добавлены узкие runtime/integration tests на contract freeze и explicit absence из agent-visible surface, а active exec-plan checklist выровнен под фактически завершённые пункты `Package A`.

## 2026-03-31 18:05

- Структура `docs/exec-plans` приведена в порядок по фактическому lifecycle: завершённые планы `windows.uia_snapshot`, `windows.wait`, `okno.health + runtime guard layer`, `Okno Codex Plugin Surface` и `okno-safety-baseline` перенесены из `docs/exec-plans/active/` в `docs/exec-plans/completed/`, чтобы в `active` оставались только действительно живые workstream-ы.
- Архивированные планы получили явные `Статус: completed` и `Архивирован: ...`, а ссылки в changelog и self-reference внутри `windows.uia_snapshot` синхронизированы с новыми путями.

## 2026-03-31 16:31

- В repo docs зафиксирована отдельная compatibility track для OpenAI `computer use` без изменения ближайшего delivery order V1: `README`, product docs, architecture index и plugin README теперь явно разводят `shell`, `skills`, `MCP`, `computer use` и `Okno` по слоям ответственности вместо implicit chat-only договорённости.
- Добавлен новый source-of-truth документ [docs/architecture/openai-computer-use-interop.md](docs/architecture/openai-computer-use-interop.md), который фиксирует adapter boundary, vocabulary expectations для будущего `windows.input`, split `windows.launch_process` / `windows.open_target` и правило, что OpenAI-specific interop не должен протекать внутрь `WinBridge.Runtime` / `WinBridge.Server`.
- В roadmap и exec-plans интегрирован planned compatibility track: [docs/product/okno-roadmap.md](docs/product/okno-roadmap.md) теперь явно удерживает `computer use` interop вне ближайшего `launch -> input -> clipboard` delivery order, а [docs/exec-plans/completed/completed-2026-04-27-openai-computer-use-interop-superseded.md](docs/exec-plans/completed/completed-2026-04-27-openai-computer-use-interop-superseded.md) задавал activation criteria для будущего adapter-слоя после shipped `windows.input(click first)`.
- `AGENTS.md` получил минимальный reusable guardrail по этой теме: source of truth для OpenAI interop теперь явно привязан к architecture/exec-plan docs, а future `computer use` compatibility закреплена как отдельный adapter-слой без протекания OpenAI-specific contracts в core runtime и без подмены repo-local MCP/plugin path.

## 2026-03-31 10:30

- Закрыт `Package E` и вместе с ним весь workstream [docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md](docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md): shared launch-readiness policy перенесена из placeholder-модели в reusable guard-layer decision matrix без реализации `windows.launch_process`, `RuntimeGuardPolicy.BuildLaunch(...)` теперь честно различает `ready / degraded / blocked / unknown` по existing runtime facts (`desktop_session`, `session_alignment`, `integrity`), а `launch_elevation_boundary_unconfirmed` стал warning для medium-integrity live launch path вместо unconditional hard block. В том же цикле обновлены unit/integration tests на launch matrix, `okno.health` projection и synthetic gated boundary, smoke подтвердил live `launch=degraded` и отсутствие `launch` в `blockedCapabilities`, generated docs не дали tracked diff после `refresh-generated-docs.ps1`, а roadmap и checklist синхронизированы с фактическим ответом `да` на proof question для будущего `windows.launch_process`.

## 2026-03-31 15:20

- Post-baseline hardening перед следующим action slice устранил три остаточных риска, найденных review sprint: raw execution boundary теперь fail-fast запрещает вызывать policy-bearing tools без `RunGated(...)` / `RunGatedAsync(...)`, launch redaction дополнительно нормализует `executable` в runtime/completion event-data до безопасного basename вместо полного пути, а smoke helper `prepare_focus` переведён на синхронный handshake, чтобы `windows.wait(focus_is)` оставался строгим и при этом детерминированным verification-signal. В том же цикле добавлены узкие runtime tests на mandatory gate misuse и launch event-data redaction, а capability design policy теперь явно фиксирует gated-boundary invariant для tools с `ExecutionPolicy`.

## 2026-03-30 15:57

- Закрыт `Package C` из [docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md](docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md) без захода в `Package D`: в `WinBridge.Runtime.Diagnostics` добавлен минимальный tool-aware redaction seam (`AuditPayloadRedactor`, `AuditRedactionResult`, tool context resolution), `AuditLog` и `ToolExecution` переведены на sanitized request/failure path с fail-safe suppression, gated invocation теперь пишет canonical `decision`/`risk_level`/`guard_capability` markers через existing `ToolExecutionDecision`, а runtime/tool events больше не утекают raw `exception_message` в `events.jsonl` и `summary.md`. В том же цикле обновлены узкие unit/integration tests на redaction markers и отсутствие leakage для `windows.wait`, `windows.uia_snapshot` и synthetic gate boundary, плюс синхронизированы только минимально нужные architecture docs и checklist workstream-а.

## 2026-03-30 13:25

- Закрыт `Package B` из [docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md](docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md) без захода в `Package C/D`: добавлен reusable execution gate поверх existing `execution_policy` и `RuntimeGuardAssessment`, `ToolExecution` получил gated sync/async overload для canonical `allowed / blocked / needs_confirmation / dry_run_only` routing без rollout в shipped observe tools, а unit/integration tests теперь доказывают decision matrix, reuse runtime snapshot и synthetic server-boundary path без публикации нового action tool.

## 2026-03-30 11:03

- Закрыт `Package A` из [docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md](docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md) без захода в runtime gate/redaction rollout: в `WinBridge.Runtime.Tooling` добавлен typed `execution_policy` metadata layer для deferred action tools, `ToolDescriptor`/`ContractToolDescriptor` и общий export path (`okno.contract` + generated `project-interfaces`) теперь публикуют nested policy fields в canonical snake_case, а manifest заранее хранит internal-only presets для future `windows.launch_process` и `windows.open_target` без premature publication новых public tool names. В том же цикле обновлены узкие unit/integration tests на manifest/export alignment и literal set, а shipped behavior существующих tools и `okno.health` остались без изменения.

## 2026-03-30 10:20

- Добавлен новый exec-plan [docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md](docs/exec-plans/completed/completed-2026-03-31-okno-safety-baseline.md), который зафиксировал policy-first workstream после shipped reporting-first guard layer: canonical tooling metadata для future action tools, единый execution gate с `allowed / blocked / needs_confirmation / dry_run_only`, redaction-first diagnostics model, file-level integration map, official constraints, delivery packages, verification ladder и implementation checklist для follow-up `windows.launch_process`.

## 2026-03-27 08:34

- Добавлен repo-local Codex plugin `okno`: создан marketplace `.agents/plugins/marketplace.json`, plugin root `plugins/okno/.codex-plugin/plugin.json`, plugin-local MCP manifest `plugins/okno/.mcp.json`, launcher script `plugins/okno/run-okno-mcp.ps1`, plugin README и bundled skill `okno-runtime-workflow` с `agents/openai.yaml`.
- Plugin теперь публикует отдельный MCP server `okno`, который запускает уже собранный `Okno.Server.dll` через plugin-local launcher, не переписывая legacy home-level `windows` server.
- В корневом `README.md` обновлён раздел про repo-local Codex plugin и закреплено, что user-facing plugin surface использует продуктовое имя `Okno`, а repo-local MCP identity — `okno`.

## 2026-03-27 13:28

- Package D для `okno.health + runtime guard layer` закрыт как verification/docs sync wave без расширения runtime surface: raw MCP integration теперь валидирует legacy top-level summary contract и canonical projection `blockedCapabilities` / `warnings` на live MCP boundary, а `scripts/smoke.ps1` дополнительно закрепляет отсутствие выдуманного `artifactPath`, отсутствие dedicated health runtime event beyond generic `tool.invocation.*` и раннюю materialization health evidence.
- Smoke harness теперь materialize-ит health evidence в `artifacts/smoke/<run_id>/report.json` и `summary.md` сразу после успешного `okno.health`, а затем обновляет эти артефакты финальным full-run report, поэтому downstream failure больше не оставляет readiness investigation совсем без следа.
- Source-of-truth docs выровнены под фактический reporting-first rollout: roadmap больше не держит health workstream в `запланировано`, `README` и `architecture/index` описывают `okno.health` как public readiness/guard summary, `observability` явно фиксирует отсутствие dedicated health artifact/event как текущую contract boundary, а exec-plan Package D закрыт по фактическим verification/docs результатам без подмены evidence соседним repo-local plugin surface.

## 2026-03-30 11:40

- Review-driven hardening устранил несколько реальных root-cause проблем без возврата legacy/shim paths: plugin-local MCP manifest `plugins/okno/.mcp.json` больше не хранит machine-specific absolute paths и стартует через `powershell -NoProfile -NonInteractive`, launcher перенесён в plugin root как переносимая repo-local indirection, bundled skill теперь явно требует запуск repo control-plane команд из корня репозитория, а runtime guard desktop probe стал identity-aware — `Win32RuntimeGuardPlatform` теперь различает non-`Default` input desktop и policy больше не рекламирует live GUI observe paths как usable в non-active session. Дополнительно `launch` blocked summary всегда публикует intrinsic `launch_elevation_boundary_unconfirmed`, даже в otherwise healthy environment, а unit tests закрепляют эти contract edges.

## 2026-03-25 11:05

- `okno.health` расширен до contract-first readiness summary без runtime probes и hidden enforcement: `HealthResult` теперь публикует typed `Readiness`, `BlockedCapabilities` и `Warnings`, а live wording в `ToolDescriptions` и `ToolContractManifest` выровнен под conservative readiness/guard snapshot для shipped observe paths и ближайших deferred `input` / `clipboard` / `launch` capability.

## 2026-03-23 18:52

- Добавлен новый active exec-plan `okno.health + runtime guard layer`, который фиксирует следующий cross-cutting workstream после shipped `windows.uia_snapshot` и `windows.wait` как расширение существующего `okno.health`, а не как новый isolated tool slice: в плане зафиксированы boundary/non-goals, reporting-first модель readiness/guard domains, file-level integration map, official Windows/MCP constraints, delivery packages, test ladder, docs sync и implementation checklist для будущего authoritative runtime readiness snapshot.

## 2026-03-23 15:27

- `visual_changed` переведён на целевую модель `detection + best-effort evidence`: public flat contract теперь публикует `lastObserved.visualEvidenceStatus` (`materialized | timeout | failed | skipped`), baseline/current PNG paths стали optional и больше не определяют success-path, visual probe разбит на lightweight comparison sample и отдельный evidence frame, compare path materialize-ит fingerprint только внутри visual probe через direct `LockBuffer`/`IMemoryBufferByteAccess` path с официальным WinRT ABI contract без copy-based fallback в latency-sensitive poll loop, PNG evidence кодируется через WinRT `BitmapEncoder` c budget-aware async bridge, orchestration теперь делает wall-clock post-budget downgrade для late evidence completion, а `wait.runtime.completed` дополнительно пишет visual baseline/current artifact paths вместе со статусом evidence; runtime/integration tests, smoke и source-of-truth docs синхронизированы с новой semantics.

## 2026-03-23 13:07

- Review-driven hardening для shipped `windows.wait` закрыл подтверждённые семантические дыры без смены публичной schema: UIA multi-match classification теперь condition-specific (`element_exists` требует identity-overlap между candidate и recheck, `element_gone` продолжает polling пока matches остаются, `text_appears` считает только text-qualified candidates, а ambiguous result больше не публикует произвольный `matchedElement`), late UIA downgrade использует `WorkerCompletedAtUtc`, wait-specific visual probe больше не наследует скрытый `3s` capture cap и репортит effective threshold/evidence-path честно, а tool-boundary unexpected failures в `WindowTools.Wait(...)` теперь проходят через тот же canonical wait artifact + `wait.runtime.completed`, а не через голый sanitized handler path; новые runtime/integration tests закрепляют эти контракты регрессионно.

## 2026-03-23 10:02

- В локальный `AGENTS.md` добавлен точечный operational guardrail для verification loop: в этом репозитории нельзя параллелить `dotnet build/test`, `scripts/smoke.ps1`, `scripts/refresh-generated-docs.ps1`, `scripts/ci.ps1` и `scripts/codex/verify.ps1` в одном worktree, потому что они делят `bin/obj`, локальные runtime/fixture процессы и generated artifacts; canonical порядок для smoke/docs refresh теперь закреплён явно как строго последовательный.
- Review-driven hardening для `windows.wait` довёл internal diagnostics path и L3 smoke до архитектурно честного состояния: controlled helper commands теперь делают `element_gone` smoke-предусловие невакуумным, `focus_is` переставлен сразу за первый authoritative `active_window_matches` вместо позднего OS-dependent foreground restore path, UIA-based smoke waits используют отдельный semantic budget для mandatory recheck, `stack-inventory` генерируется из текущего repo/tooling state без захардкоженной snapshot-date, а canonical runtime path для `runtime_unhandled` failures сохраняет `exception_type` / `exception_message` во внутреннем wait artifact и `wait.runtime.completed`, тогда как для `artifact_write` эти diagnostics остаются только в runtime event, не раскрывая детали в публичный MCP `WaitResult`.

## 2026-03-23 08:59

- Package D для `windows.wait` закрыт как direct public rollout без сохранения legacy stub: `WindowTools.Wait(...)` теперь публикует final MCP schema `condition + selector + expectedText + hwnd + timeoutMs`, напрямую вызывает canonical `IWaitService` через existing `ResolveWaitTarget(...)`, возвращает runtime `WaitResult` как `structuredContent` + один `TextContentBlock` и строго маппит `isError` так, что только `done` остаётся success-path; `windows.wait` переведён в `Implemented` и `SmokeRequired`, `okno.contract` / exporter / tools-list больше не расходятся по lifecycle, `WinBridge.SmokeWindowHost` получил deterministic focus target и geometry-backed visual heartbeat для shipped `focus_is` / `visual_changed`, `scripts/smoke.ps1` теперь реально проверяет все public wait conditions с artifact assertions, а generated/manual docs и bootstrap status синхронизированы по фактическому shipped state.

## 2026-03-20 17:15

- Package C для `windows.wait` закрыт как runtime-only hardening без premature public rollout: `focus_is` теперь подтверждается только через authoritative focused-element path с retry/revalidation только для действительно transient `ElementNotAvailable`-класса ошибок, exact selector match внутри resolved window и корректной immediate-parent lineage metadata, `visual_changed` использует window-scoped raw-only visual probe с гарантированным положительным confirmation gap и noise policy на `16x16` grayscale grid (`per-cell delta >= 12`, populated-cell scaled threshold, geometry-change shortcut) без per-tick PNG bloat, без лишних raw allocations в обычном `windows.capture` и без удержания raw baseline frame между poll-итерациями, `WaitOptions` больше не допускает non-positive polling cadence, baseline и final visual artifact write path теперь используют общий remaining-budget helper и целиком нормализуют filesystem/encode failures в доменный `failed`, process-isolated UIA wait probe разделяет host completion и worker completion timestamps для корректного timeout enforcement, а DI/runtime tests закрепляют новый visual probe seam при сохранении lifecycle `windows.wait` в `Deferred/unsupported`.

## 2026-03-20 15:29

- Follow-up hardening закрепил ownership timeout и evidence contract на правильных границах: `PollingWaitService` теперь явно передаёт worker boundary remaining budget на каждый UIA probe и повторно классифицирует просроченный probe result как `timeout` по execution metadata, process-isolated wait probe больше не наследует скрытый snapshot-centric 3s cap, а worker-level `diagnostic_artifact_path` больше не теряется и попадает в wait observation / wait artifact / runtime audit trail.

## 2026-03-20 15:50

- Финальная review-driven правка для Package B убрала смешение deadline и failure semantics в `PollingWaitService`: service теперь сначала строит семантический outcome UIA probe (`failed` / `ambiguous` / `pending` / `candidate` / `timeout`) и только потом понижает просроченный результат до `timeout` для success-like веток, поэтому late `worker_process` и другие runtime failures больше не маскируются под `timeout`, а существующий red-test на delayed failure фиксирует этот контракт регрессионно.

## 2026-03-20 15:29

- Follow-up hardening закрепил ownership timeout и evidence contract на правильных границах: `PollingWaitService` теперь явно передаёт worker boundary remaining budget на каждый UIA probe и повторно классифицирует просроченный probe result как `timeout` по execution metadata, process-isolated wait probe больше не наследует скрытый snapshot-centric 3s cap, а worker-level `diagnostic_artifact_path` больше не теряется и попадает в wait observation / wait artifact / runtime audit trail.

## 2026-03-20 14:43

- Review-driven доработка execution boundary довела timeout hardening до архитектурно честного состояния: UIA wait probe больше не остаётся внутрипроцессным cooperative-only MTA path, а исполняется через process-isolated worker boundary, общий по launch/timeout/kill semantics с существующим isolated UIA slice; это убирает расхождение между reported `timeout` и фактическим завершением blocked UIA/COM execution unit.

## 2026-03-20 14:12

- Review-driven hardening для Package B убрал три реальные P1-дыры в `windows.wait`: UIA-backed probes теперь bounded оставшимся `timeoutMs` через отдельную deadline policy на execution boundary, `active_window_matches` подтверждает именно resolved top-level `HWND`, а `text_appears` требует тот же `matched_text_source` на финальном recheck; для всех трёх случаев добавлены red-first runtime tests, чтобы regression path был доказуемо закрыт.

## 2026-03-20 11:14

- Package B для `windows.wait` закрыт как runtime-only slice без premature public rollout: wait contracts перешли на typed selector + expectedText shape, `PollingWaitService` добавил bounded poll loop с final same-source revalidation и честным разделением `done` / `timeout` / `ambiguous` / `failed`, `Windows.UIA` получил минимальный live wait probe seam для `active_window_matches`, `element_exists`, `element_gone` и `text_appears`, а diagnostics слой теперь пишет отдельный JSON artifact в `artifacts/diagnostics/<run_id>/wait/` и runtime audit event `wait.runtime.completed`, сохраняя `windows.wait` в lifecycle `Deferred/unsupported` до Package D.

## 2026-03-20 09:16

- Package A для `windows.wait` закодирован без premature rollout: добавлены typed `Wait*` contracts, `IWaitService` больше не пустой, shell resolver получил capability-specific `ResolveWaitTarget(...)` с precedence `explicit -> attached -> active` и без fallback из stale explicit/attached target, а deferred manifest/exported contract теперь честно помечает `windows.wait` как `os_side_effect`, сохраняя lifecycle `Deferred`.

## 2026-03-20 08:30

- Source-of-truth docs для следующего slice `windows.wait` выровнены между exec-plan, product spec и roadmap: V1 теперь везде описан как один публичный tool `windows.wait`, а не zoo из `wait_for_*`; target model синхронизирован как `explicit -> attached -> active` без hidden activation/auto-attach drift, summary-row roadmap больше не теряет `text appears`, а сам exec-plan честно меняет MCP annotation expectation на `ReadOnly = false`, потому что wait обязан писать diagnostics artifact.

## 2026-03-19 18:23

- Добавлен отдельный exec-plan для shipped public slice `windows.wait` в [docs/exec-plans/completed/completed-2026-03-23-windows-wait.md](docs/exec-plans/completed/completed-2026-03-23-windows-wait.md): план фиксирует V1 boundary, polling-first target model `explicit -> attached -> active`, condition matrix для `active/focus/element/text/visual`, honest status/error model, evidence contract, file-level integration map, L1/L2/L3 ladder, docs sync и rollback без преждевременного захода в `windows.uia_action` или broad `windows.input`.

## 2026-03-19 14:34

- Финальный branch-аудит по `windows.uia_snapshot` добрал остаточный doc drift вне основного rollout-контура: `architecture/index`, `architecture/layers`, `product/okno-roadmap`, `generated/stack-inventory` и `generated/stack-research` больше не описывают UIA как future seam/deferred path там, где текущий код уже публикует shipped public `windows.uia_snapshot`.
- Review-driven hardening для `windows.uia_snapshot` закрыл четыре корневые проблемы public rollout сразу на boundary-уровне: shared `UiaSnapshotRequestValidator` теперь одинаково валидирует request в server/runtime и держит публичный `maxNodes` в диапазоне `1..1024`, runtime service больше не публикует stale pre-resolution descriptor и теперь берет `window` только как sparse runtime-observed metadata из того же backend capture path, который построил фактический root/subtree, diagnostics boundary получил отдельный sanitized-failure path с сохранением `exception_type`/`exception_message` в audit trail, а smoke-контуру добавлен polling до materialized semantic subtree вместо single-shot UIA assertions.

## 2026-03-19 13:25

- `windows.uia_snapshot` доведён до shipped public slice: `Okno.Server` теперь публикует live MCP handler с `hwnd + depth + maxNodes`, `CallToolResult`, `structuredContent` и одним `TextContentBlock` без image block; target policy `explicit -> attached -> active` честно резолвится через existing shell seam, а stale/ambiguous target cases больше не маскируются deferred stub-ом.
- Public rollout осознанно изменил server runtime contract: `Okno.Server` получил host-facing зависимость на `WinBridge.Runtime.Windows.UIA.Hosting`, runtime config теперь legitimately включает `Microsoft.WindowsDesktop.App`, а boundary protection перенесён из pre-rollout запрета в positive guard на staged UIA worker artifacts и новый WindowsDesktop-backed shipped state.
- Smoke и generated/docs слой выровнены под live contract: helper window теперь содержит предсказуемый semantic subtree на WinForms standard controls, `McpProtocolSmokeTests` и `scripts/smoke.ps1` проверяют attached-source UIA snapshot и JSON evidence artifact, `project-interfaces`/`bootstrap-status`/`test-matrix` больше не держат UIA в deferred scope, а `observability`/`okno-spec`/`okno-roadmap` синхронизированы по фактическому shipped behavior.

## 2026-03-19 11:16

- Review-driven hardening для `windows.uia_snapshot` развёл semantics bounded traversal по двум независимым границам: `UiaSnapshotTreeBuilder` больше не зондирует child nodes после исчерпания `MaxNodes`, `Truncated` остаётся только флагом доказанного clipping, а новый `node_budget_boundary_reached` фиксирует strict no-probe budget boundary так же явно, как `depth_boundary_reached` фиксирует depth boundary.
- Isolated worker transport boundary доведён до evidence-grade поведения: parent/worker stdio теперь жёстко зафиксированы на UTF-8, stdin/process/payload failures маппятся в typed `worker_process` result без unhandled leak, raw worker stderr больше не публикуется как operator-facing `Reason`, а сохраняется в отдельный diagnostic artifact с `diagnostic_artifact_path` в runtime audit event.
- Worker packaging переведён на publish-aware consumer graph: `WinBridge.Runtime.Windows.UIA.Worker` включён в hosting graph как non-copying sidecar dependency, build и publish path stage-ят build/published artifacts раздельно, а runtime launcher теперь использует явный dual launch contract (`worker.exe` или apphost-less `worker.dll` через текущий `dotnet` host), поэтому valid framework-dependent `UseAppHost=false` consumer publish больше не ломает UIA sidecar path.

## 2026-03-19 09:51

- Consumer-facing DI boundary для `windows.uia_snapshot` доведён до self-contained registration contract: в `WinBridge.Runtime.Diagnostics` выделен общий `AddWinBridgeRuntimeDiagnostics(...)`, `WinBridge.Runtime` и `WinBridge.Runtime.Windows.UIA.Hosting` переиспользуют один и тот же diagnostics/time-provider fragment, а `AddWinBridgeRuntimeWindowsUia(...)` теперь явно принимает `contentRootPath` и `environmentName` вместо скрытой зависимости от чужого composition root.
- Добавлен минимальный consumer regression test на разрешение `IUiAutomationService` через hosting boundary без ручного дотягивания `AuditLog`/`AuditLogOptions`/`TimeProvider`; staging worker-а и DI resolution теперь проверяются как один согласованный hosting contract.

## 2026-03-19 09:30

- Follow-up hardening для `windows.uia_snapshot` довёл packaging/runtime boundary до consumer-facing контракта: `WinBridge.Runtime.Windows.UIA.Hosting` теперь stage-ит `WinBridge.Runtime.Windows.UIA.Worker.exe/.dll/.deps.json/.runtimeconfig.json` в собственный output и в output потребителей через явный MSBuild target, поэтому helper worker больше не появляется случайно только из-за прямых test references.
- В том же цикле deployment failure перестал ломать DI construction: `ProcessIsolatedUiAutomationBackend` проверяет наличие worker-а только на execution path и переводит misdeployment в `failed` result + `uia.snapshot.runtime.completed`, а `UiAutomationMtaRunner` закреплён как cooperative MTA utility, тогда как единственным strict-timeout owner остаётся isolated process boundary.

## 2026-03-18 16:58

- `windows.uia_snapshot` получил только Package B runtime/evidence слой без premature public rollout: добавлены `Win32UiAutomationService`, managed UIA backend на отдельном MTA thread, `ElementFromHandle` root acquisition, bounded `control view` traversal, enforcement `Depth`/`MaxNodes`/`Truncated` и JSON artifact под `artifacts/diagnostics/<run_id>/uia/`.
- Follow-up hardening этого же пакета убрал unintended host dependency drift и diagnostic gaps: UIA registration вынесена в отдельный host-facing project `WinBridge.Runtime.Windows.UIA.Hosting`, bootstrap `Okno.Server` снова не требует `Microsoft.WindowsDesktop.App` до Package C, worker artifact больше не ищется в `repo Debug` layout, production execution boundary переведена на isolated worker process, evidence shape теперь включает `requested_max_nodes`, `depth_boundary_reached` и `failure_stage`, а tree builder больше не перегружает `Truncated` ложноположительными depth-boundary срабатываниями.

## 2026-03-18 16:04

- Ещё один review-driven hardening проход довёл UIA target policy до строгой семантики explicit target: `explicitHwnd <= 0` в `windows.uia_snapshot` больше не нормализуется в “target absent”, а возвращает явный `stale_explicit_target`; при этом общий resolver для существующих `focus/capture` путей остаётся без этого UIA-specific правила.
- Deterministic generation для tracked docs усилена до byte-level policy: `scripts/refresh-generated-docs.ps1` теперь не только пишет фиксированный UTF-8, но и нормализует line endings, а `bootstrap-status.json` получает полный JSON string escaping без runtime-dependent serializer drift между `powershell.exe` и `pwsh`.

## 2026-03-18 15:41

- Review-driven hardening локализовал `explicitHwnd <= 0` обратно в `windows.uia_snapshot` policy path: общий `ResolveExplicitOrAttachedWindow(...)` больше не меняет semantics для существующих `windows.focus_window` и `windows.capture`, а regression tests теперь явно страхуют `hwnd: 0` при attached session window.
- `scripts/refresh-generated-docs.ps1` доведён до кросс-shell deterministic generation: tracked generated files теперь пишутся через общий UTF-8 writer с фиксированной BOM-policy, а `bootstrap-status.json` больше не зависит от различий `ConvertTo-Json` между `powershell.exe` и `pwsh`.

## 2026-03-18 15:22

- Review-driven fix для harness encoding вернул `scripts/refresh-generated-docs.ps1` в совместимый с Windows PowerShell режим: `.editorconfig` теперь фиксирует `utf-8-bom` для `*.ps1`, а сам скрипт повторно сохранён с BOM, чтобы `powershell.exe -File` больше не падал на non-ASCII литералах.
- В том же цикле deterministic generated docs остались без run-specific smoke metadata, но `refresh-generated-docs.ps1`, `ci.ps1` и `scripts/codex/verify.ps1` снова проходят именно через стандартный Windows PowerShell execution path, а не только через текущую `pwsh`-сессию Codex.

## 2026-03-18 12:59

- `windows.uia_snapshot` переведён на package-aware execution plan: active exec-plan теперь явно разделяет `Package A` (target policy + typed groundwork), `Package B` (runtime/evidence) и `Package C` (server rollout/smoke/generated docs), чтобы текущий цикл больше не притворялся полным rollout до `Implemented`.
- Для `windows.uia_snapshot` зафиксировано official-docs-driven meaning `active = foreground top-level window`, а product wording в spec/roadmap выровнен под precedence `explicit -> attached -> active` без преждевременного опубликования live MCP contract.
- В коде добавлены typed `UiaSnapshot*` DTO, typed `IUiAutomationService` seam и capability-specific `UiaSnapshotTargetResolution`; при этом public handler, manifest lifecycle и generated docs намеренно оставлены в честном deferred state.

## 2026-03-18 15:10

- Review-driven hardening для `windows.uia_snapshot` убрал внутреннюю двусмысленность groundwork: service-level request больше не несёт второй authoritative target, defaults централизованы в `UiaSnapshotDefaults`, а `UiaSnapshotResult` больше не может родиться с implicit success status.
- Active-path policy в `WindowTargetResolver` сделана snapshot-consistent: explicit `hwnd <= 0` нормализуется как absent target, active candidate выбирается из одного `ListWindows` snapshot, duplicate foreground entries по тому же `HWND` дедуплицируются, а `missing_target` и `ambiguous_active_target` больше не смешиваются.
- `scripts/refresh-generated-docs.ps1`, `docs/generated/*`, `docs/bootstrap/bootstrap-status.json` и tool contract export переведены на deterministic generation: tracked docs больше не содержат run-specific smoke ids, absolute artifact paths и `generated_at_utc`, а конкретное evidence остаётся только в локальных `artifacts/`.

## 2026-03-18 11:08

- Добавлен отдельный exec-plan для shipped capability slice `windows.uia_snapshot` в [docs/exec-plans/completed/completed-2026-03-19-windows-uia-snapshot.md](docs/exec-plans/completed/completed-2026-03-19-windows-uia-snapshot.md): план фиксирует contract-first goal, non-goals, official Microsoft/MCP constraints, file-level integration map, typed DTO/result shape, L1/L2/L3 ladder, docs sync и rollback policy без преждевременного захода в `windows.wait`, `windows.input` или `windows.uia_action`.
- В тот же exec-plan добавлена явная target policy `explicit -> attached -> active` для `windows.uia_snapshot` с запретом на silent fallback из stale attached/explicit target и расширен обязательный docs-sync до полного source-of-truth набора по slice: `product/index`, `okno-spec`, `okno-roadmap`, `okno-vision`, `observability` и generated docs.

## 2026-03-18 09:50

- `docs/product/okno-roadmap.md` переведён из чисто greenfield narrative в repo-aligned operational roadmap: документ теперь явно фиксирует текущий реализованный bootstrap baseline (`Server` / `Contracts` / `Diagnostics` / `Session` / `Windows.Display` / `Windows.Shell` / `Windows.Capture`) и различает `реализовано`, `частично`, `только_seam` и `запланировано` для capability slices.
- В roadmap добавлена таблица приоритетов в реальных именах репозитория и tool surface, чтобы execution order больше не расходился с текущим contract/runtime: следующий рубеж формулируется как `windows.uia_snapshot -> windows.wait -> environment/safety -> launch -> input`, а не как старый абстрактный порядок из greenfield-версии.
- Порядок приоритета при нехватке времени и итоговая формулировка V1 выровнены под текущий repo state: `launch` разделён на `windows.launch_process` и `windows.open_target`, а преждевременное расширение `windows.input` прямо зафиксировано как риск до появления snapshot/wait/guard layers.
- `docs/product/okno-vision.md` и `docs/product/okno-spec.md` дополнительно выровнены по concrete tool naming: V1 core tools теперь используют `windows.list_monitors`, `windows.focus_window`, `windows.activate_window`, `windows.clipboard_get` и `windows.clipboard_set`, а clipboard/paste терминология больше не расходится между vision/spec/roadmap.

## 2026-03-17 16:07

- WGC one-shot capture path больше не считает первый пришедший frame автоматически годным: runtime теперь валидирует `Direct3D11CaptureFrame.ContentSize`, допускает ровно один `Direct3D11CaptureFramePool.Recreate(...)` и сохраняет PNG только после стабилизации геометрии кадра.
- Persistent WGC size drift после single `Recreate` теперь считается отдельным acquisition failure class: для `desktop` runtime уходит в существующий `Graphics.CopyFromScreen` fallback, а для `window` возвращает честный tool-level error без подмены window semantics screen-copy screenshot'ом.
- Metadata и artifact materialization больше не опираются на pre-acquisition target snapshot: после WGC stabilization runtime пере-выравнивает target fields под финальный PNG, а перед desktop fallback заново резолвит текущий monitor target вместо silent reuse устаревших bounds.
- Для успешного `desktop` WGC path monitor identity больше не может silently перепрыгнуть на новый live monitor после refresh: refreshed topology используется только если она подтверждает тот же target, по которому уже был создан capture item.
- Добавлены unit tests на sizing/failure policy и authoritative target materialization, чтобы WGC stabilization не расходилась с MCP metadata path.

## 2026-03-17 10:30

- Review-driven hardening закрыл подтверждённые P1/P2 gaps: MCP server теперь явно включает per-monitor DPI awareness в startup bootstrap, а integration smoke ждёт достижение этого process invariant перед валидацией contract.
- Display identity diagnostics переведены на отдельный builder/state machine: query failures больше не могут ложно репортиться как `display_config_strong`, а деградация `GetTargetName` теперь сохраняется в typed diagnostics без понижения strong identity до `gdi_fallback`.
- `okno.contract` обогащён до структурированных `ContractToolDescriptor` вместо списка имён, а `ToolContractManifest` и generated interfaces теперь публикуют те же полные описания, что и MCP `tools/list`.
- Manual docs синхронизированы с новым contract: `observe-capture`, `observability` и `okno-spec` больше не описывают удалённый monitor `dpi scale` и теперь отражают `coordinateSpace`, window-authoritative DPI и display identity diagnostics.

## 2026-03-17 10:44

- Закрыт residual bug в `DisplayIdentityDiagnosticsBuilder`: mixed-case `GetTargetName` degradation + `gdi_fallback` больше не может выдавать противоречивое сообщение в духе "strong identity preserved"; добавлен red/green unit test на этот сценарий.

## 2026-03-17 10:53

- Stage 3 contract surface доведён до консистентности: `okno.contract` и export/generated contract теперь используют общий `ContractToolDescriptorFactory`, а enum-like поля `lifecycle` и `safetyClass` публикуются в одном canonical snake_case literal format без drift между live tool и exported interfaces.

## 2026-03-17 11:40

- Review-driven closeout для workstream 1/2/3 завершён: `Win32WindowManager` больше не маскирует `GetDpiForWindow == 0` значением `96`, coverage-driven `gdi_fallback` теперь получает typed reason `display_config_coverage_gap`, а manual spec `windows.list_monitors` / `windows.list_windows` синхронизирована с фактическими diagnostics и window DPI fields.

## 2026-03-17 12:15

- Verification gaps закрыты локальными test seams: добавлен pipeline-level runtime test на mixed-case display failures (`GetTargetName` -> `GetSourceName`) и behavioural integration test на `windows.capture(scope="desktop", hwnd=...)`, подтверждающий monitor resolution explicit HWND поверх attached window. Smoke по-прежнему проверяет tools/list metadata и end-to-end protocol flow, а не подменяет эти два таргетных контракта.

## 2026-03-17 09:39

- Перестроен display/window contract: `MonitorDescriptor` больше не несёт authoritative `DpiScale`, `WindowDescriptor` теперь содержит `EffectiveDpi`, а `windows.capture` возвращает `coordinateSpace=physical_pixels` и window-authoritative DPI metadata только для window targets.
- `WinBridge.Runtime.Windows.Display` теперь возвращает typed `DisplayTopologySnapshot` с `DisplayIdentityDiagnostics`; `windows.list_monitors`, `okno.health`, audit `events.jsonl` и summary получили evidence о `display_config_strong` vs `gdi_fallback` без изменения safe fallback behavior.
- MCP surface стал самодокументируемым: добавлен единый `ToolDescriptions` source of truth, `DescriptionAttribute` на ключевые tools/parameters и smoke/integration checks на наличие descriptions в `tools/list`.
- `IsWindowArranged` переведён в optional metadata probe через runtime export lookup, чтобы `windowState=arranged` оставался enrichment-сигналом и не создавал жёсткой платформенной зависимости inventory.

## 2026-03-17 15:15

- `refresh-generated-docs.ps1` больше не публикует ложный `green` в `docs/generated/test-matrix.md`: матрица стала декларативной, а не evidence-claiming. В том же цикле добавлена `Normalize-SmokeReport`, чтобы generated docs не падали на legacy/partial smoke reports, а `GetMonitorInfo` degradation теперь поднимается в typed diagnostics вместо тихого skip-case.

## 2026-03-17 09:21

- Добавлен exec-plan `docs/exec-plans/completed/completed-2026-03-17-display-window-contract-hardening.md` для линейного refactor-контура по DPI/coordinate semantics, display identity diagnostics, MCP self-documentation и optional `IsWindowArranged`.

## 2026-03-15 14:57

- `windows.activate_window` теперь маркирует `ambiguous` как tool-level error на MCP boundary, а не как успешный result.
- `WinBridge.Server.IntegrationTests` получил явный build dependency на `WinBridge.SmokeWindowHost`, чтобы deterministic helper window собирался вместе с integration graph.
- Display seam переведён на source/view-oriented `monitorId` для captureable desktop views; alias fallback для stale strong ids убран, а `windows.list_windows` перешёл на batch-friendly monitor lookup без full scan на каждый HWND.
- Smoke harness переведён на helper как канонический attach/capture target: сначала normal window capture, затем `minimize -> activate_window -> helper capture`, при этом acceptance для activation path выровнен с платформой как `done | ambiguous` вместо безусловочного `done`.

## 2026-03-16 14:58

- `windows.activate_window` теперь возвращает `done` только когда финальный live snapshot подтверждает usable state: окно остаётся foreground и уже не находится в `minimized`.
- `WinBridge.Runtime.Windows.Display` зарегистрирован как полноценный runtime slice в `WinBridge.sln`, а `scripts/refresh-generated-docs.ps1` больше собирает `runtime_projects` автоматически по `WinBridge.Runtime*` csproj, чтобы bootstrap inventory не дрейфовал от фактической архитектуры.

## 2026-03-16 15:55

- Helper-based smoke harness переведён на contract-true readiness: helper считается готовым только после появления в `windows.list_windows(includeInvisible=false)`, а не сразу после появления `MainWindowHandle`.
- Post-activation helper capture в smoke и raw MCP integration smoke теперь ждёт фактическую capturable ready-state через `windows.capture(scope="window")`, вместо безусловочного принятия любого `ambiguous` как достаточного для следующего шага.

## 2026-03-16 16:14

- Smoke harness и raw MCP integration smoke переведены на session-scoped JSON-RPC request ids и строгий response matching по ожидаемому `id`, чтобы retry-циклы не могли переиспользовать корреляционные идентификаторы и ловить ответы не от своей фазы.

## 2026-03-16 16:56

- `windows.activate_window` больше не собирает final verdict из разрозненных raw-handle probes: финальная verification теперь опирается на единый platform-backed snapshot с identity (`ProcessId`/`ThreadId`/`ClassName`) и usability (`IsForeground`/`IsMinimized`) signals, а `done` выдаётся только по нему.

## 2026-03-15 12:36

- `windows.attach_window` теперь разводит invalid selector и ambiguous match: отсутствие `hwnd`/`titlePattern`/`processName` возвращается как `failed`, а `ambiguous` остаётся только для реального multi-match path.
- Добавлены integration tests на `windows.focus_window` и contract semantics `windows.attach_window`, включая success/failure paths, `already_attached` и гарантию, что focus не мутирует session state.
- `docs/generated/test-matrix.md` переведён в generated-артефакт через `scripts/refresh-generated-docs.ps1`, чтобы coverage-summary не расходился с `commands.md`.

## 2026-03-15 13:58

- Добавлен новый `Windows.Display` seam с monitor inventory и strong `monitorId` на основе `QueryDisplayConfig` + `DisplayConfigGetDeviceInfo`, а `windows.list_windows` и `windows.capture` теперь становятся monitor-aware.
- Добавлены новые MCP tools `windows.list_monitors` и `windows.activate_window`; `windows.capture(scope="desktop")` теперь поддерживает explicit `monitorId`, а `window capture` для minimизированного окна честно требует предварительный `activate_window`.
- Smoke harness и raw stdio integration smoke расширены до deterministic helper-window сценария: explicit desktop capture по `monitorId`, `minimize -> activate_window -> window capture`.

## 2026-03-12 16:16

- Инициализирован harness bootstrap: созданы `docs/`, ExecPlan, верхнеуровневый `AGENTS.md`, solution/project skeleton и baseline-конфиг для `net8.0-windows`.
- Добавлен рабочий `STDIO` MCP skeleton runtime на `C# / .NET 8`, включая `windows.list_windows`, `windows.attach_window`, `windows.focus_window` и честный deferred tool contract.
- Нормализованы `scripts/bootstrap.ps1`, `scripts/build.ps1`, `scripts/test.ps1`, `scripts/smoke.ps1`, `scripts/ci.ps1`, `scripts/investigate.ps1` и `scripts/codex/*`.
- Проверки подтверждены командами `dotnet build`, `dotnet test`, `powershell -File scripts/smoke.ps1` и `powershell -File scripts/ci.ps1`.
- Продуктовое имя выровнено на `Okno`, product docs перенесены в `docs/product/`, добавлены `README.md` и `docs/architecture/engineering-principles.md`.

## 2026-03-12 21:02

- Реализован первый observe-loop `windows.list_windows -> windows.attach_window -> windows.capture` с `CallToolResult`, `structuredContent`, `image/png` и локальным PNG artifact в `artifacts/diagnostics/<run_id>/captures/`.
- `windows.capture` переведён из deferred в implemented tool contract, добавлен в smoke-required контур и покрыт server-side/integration тестами.
- Для `window` и `desktop` capture выбран `Windows.Graphics.Capture` как основной backend, с native GDI fallback для target-specific случаев, где WGC не даёт стабильный frame вовремя.
- `scripts/smoke.ps1`, generated docs и bootstrap status синхронизированы под новый observe/capture slice.

## 2026-03-13 14:15

- Добавлена универсальная policy для проектирования новых capability slices в [docs/architecture/capability-design-policy.md](architecture/capability-design-policy.md): identity, lifecycle, fallback, false-success, evidence и verification matrix.
- `architecture/index`, `engineering-principles` и `AGENTS.md` теперь явно ссылаются на эту policy как на обязательный baseline для следующих `focus`, `clipboard`, `input`, `wait`, `uia` и других feature.

## 2026-03-13 09:40

- Ужесточена семантика `window capture`: minimизированный `HWND` теперь считается явной ошибкой, а не допустимым success target.
- Fallback policy разделена по `scope`: `Graphics.CopyFromScreen` оставлен только для `desktop monitor` path, но больше не подменяет `window` semantics.
- Desktop fallback теперь достигается и в `WGC unsupported` средах, а не только после timeout/native error в WGC path.
- Untargeted `desktop` capture теперь явно выбирает primary monitor через Win32 monitor enumeration, а не через virtual-origin heuristic.
- `desktop` capture теперь корректно падает в primary monitor и при stale attached window, вместо того чтобы требовать ручного исправления session state.
- Attached target теперь переоценивается по `HWND + ProcessId + ThreadId + ClassName`; `Title` снова не считается обязательной частью identity.
- MCP annotations для `windows.capture` выровнены с реальным поведением: tool больше не `readOnly`/`idempotent`, и теперь помечен как `openWorld`.
- Expected operational failures в capture path нормализованы в `CaptureOperationException`, чтобы клиент получал tool-level `isError=true`, а не transport-level exception.
- `FrameArrived` теперь освобождает лишние `Direct3D11CaptureFrame`, а smoke/integration selection выбирают candidate через реальные attach/capture success criteria вместо геометрических эвристик.
