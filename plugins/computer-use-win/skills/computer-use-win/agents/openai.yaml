interface:
  display_name: "Okno: Computer Use for Windows"
  short_description: "State-first workflow on top of the public computer-use-win MCP surface"
  brand_color: "#0F766E"
  default_prompt: "Get the current app state and click through the GUI step by step"
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "computer-use-win"
      description: "Plugin-local Computer Use for Windows MCP surface powered by the internal Okno engine"
